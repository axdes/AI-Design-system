# Change log

The working log of this package lives in its source monorepo and is not
published: it narrates client projects alongside the system. What changed in a
published snapshot is what its commit says; the reasoning that matters for
USING the system lives in AGENTS.md, the registry and the golden examples.
