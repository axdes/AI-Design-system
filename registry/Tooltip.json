{
  "ref": "Tooltip",
  "level": "molecule",
  "description": "Text on hover and focus, and the only right answer for naming an icon-only control. Popover is a card on click, Dropdown is a menu. Copy: names the control, it does not explain it. If the words are needed to decide, they belong on the page — a tooltip is unreachable on a touch screen and gone on the next keystroke.",
  "exports": [
    "Tooltip"
  ],
  "main": "Tooltip",
  "from": "@/components/Tooltip",
  "slots": [],
  "props": [
    {
      "name": "content",
      "type": "string",
      "required": true,
      "description": "What the control is CALLED — one or two words, the same verb it would carry if it had room. Not an explanation: a tooltip cannot be reached by touch, cannot be selected, and disappears the moment the pointer leaves, so anything the reader actually needs belongs on the page. On an icon-only control this is the VISIBLE name and `aria-label` is the announced one; both, always."
    },
    {
      "name": "placement",
      "type": "Placement",
      "required": false,
      "description": "Which side it prefers, chosen by the room available rather than the look — `start`/`end` beside a control at the edge of a panel, where a tooltip above it would be clipped. A preference, not an instruction: the layer flips itself when the chosen side does not fit.",
      "values": [
        "top",
        "bottom",
        "end",
        "start"
      ]
    },
    {
      "name": "delay",
      "type": "number",
      "required": false,
      "description": "Hover delay before the tooltip appears (ms). Default 300."
    },
    {
      "name": "enabled",
      "type": "boolean",
      "required": false,
      "description": "When false, renders children untouched. Default true."
    },
    {
      "name": "children",
      "type": "ReactElement<ChildProps>",
      "required": true
    }
  ],
  "sourcePath": "src/components/Tooltip/Tooltip.tsx",
  "context": "region",
  "uses": [
    "Button",
    "IconButton",
    "Layout"
  ],
  "example": "<Row gap={3} align=\"center\">\n  <Tooltip content=\"Duplicate\">\n    <IconButton icon=\"content_copy\" aria-label=\"Duplicate\" onClick={() => undefined} />\n  </Tooltip>\n  <Tooltip content=\"Archive\" placement=\"bottom\">\n    <IconButton icon=\"folder\" aria-label=\"Archive\" onClick={() => undefined} />\n  </Tooltip>\n  <Tooltip content=\"Saves as you type\" placement=\"end\">\n    <Button variant=\"secondary\">Autosave</Button>\n  </Tooltip>\n</Row>"
}
