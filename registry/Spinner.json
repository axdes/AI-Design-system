{
  "ref": "Spinner",
  "level": "atom",
  "description": "The one busy indicator for the whole system: a rotating loader icon. Inline by default (sits next to text or inside a button); center it with the layout around it. Animation is paused under prefers-reduced-motion (reset.css). Copy: the label says what is being waited for — \"Loading invoices\" — and it is announced, so it is a sentence a person can hear.",
  "exports": [
    "Spinner"
  ],
  "main": "Spinner",
  "from": "@/components/Spinner",
  "slots": [],
  "props": [
    {
      "name": "size",
      "type": "Size",
      "required": false,
      "description": "Matches the icon scale: sm 16 / md 20 / lg 24 / xl 40. Default 'md'.",
      "values": [
        "sm",
        "md",
        "lg",
        "xl"
      ]
    },
    {
      "name": "label",
      "type": "string",
      "required": true,
      "description": "Accessible name announced to screen readers. A spinner with no label is a silent \"something is happening\" — always say what."
    },
    {
      "name": "className",
      "type": "string",
      "required": false
    }
  ],
  "sourcePath": "src/components/Spinner/Spinner.tsx",
  "variants": {
    "size": {
      "prop": "size",
      "values": [
        "sm",
        "md",
        "lg",
        "xl"
      ]
    }
  },
  "uses": [
    "Icon",
    "Layout"
  ],
  "example": "<Row gap={6} align=\"center\">\n  <Spinner label=\"Saving the draft\" />\n  <Spinner size=\"md\" label=\"Loading invoices\" />\n  <Spinner size=\"lg\" label=\"Preparing the report\" />\n</Row>"
}
