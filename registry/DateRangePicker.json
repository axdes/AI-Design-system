{
  "ref": "DateRangePicker",
  "level": "molecule",
  "description": "A date-span field: one trigger, one <Calendar>, two picks. The first click marks the start, the second closes with the ordered range (picking backwards swaps the ends instead of failing). One date is <DatePicker>; this is the from/to filter over a list or a report. Copy: the label names the span — \"Reporting period\" — and never says \"from/to\", which the two halves already say.",
  "exports": [
    "DateRangePicker"
  ],
  "main": "DateRangePicker",
  "from": "@/components/DateRangePicker",
  "slots": [],
  "props": [
    {
      "name": "value",
      "type": "DateRange",
      "required": false,
      "fields": [
        {
          "name": "start",
          "type": "Date",
          "required": true
        },
        {
          "name": "end",
          "type": "Date",
          "required": true
        }
      ]
    },
    {
      "name": "onChange",
      "type": "(range: DateRange) => void",
      "required": true,
      "description": "Fires once both ends are picked, already ordered."
    },
    {
      "name": "min",
      "type": "Date",
      "required": false
    },
    {
      "name": "max",
      "type": "Date",
      "required": false
    },
    {
      "name": "label",
      "type": "string",
      "required": true,
      "description": "Accessible name for the trigger."
    },
    {
      "name": "placeholder",
      "type": "string",
      "required": false
    },
    {
      "name": "locale",
      "type": "string",
      "required": false,
      "description": "BCP-47 locale for the shown dates + the calendar. Defaults to document lang."
    },
    {
      "name": "invalid",
      "type": "boolean",
      "required": false,
      "description": "A STATE, not a style: it turns the border and hands <Field> the hook it needs to read the error out as part of the field."
    },
    {
      "name": "surface",
      "type": "'base' | 'muted'",
      "required": false,
      "description": "Which surface the field sits on; same contract as <DatePicker>.",
      "values": [
        "base",
        "muted"
      ]
    },
    {
      "name": "size",
      "type": "'sm' | 'md' | 'lg'",
      "required": false,
      "description": "sm / md (default) / lg — matches the shared control height/padding scale.",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    {
      "name": "disabled",
      "type": "boolean",
      "required": false,
      "description": "Dimmed and unpressable, but pointer events are KEPT so a Tooltip can say why."
    },
    {
      "name": "className",
      "type": "string",
      "required": false
    }
  ],
  "sourcePath": "src/components/DateRangePicker/DateRangePicker.tsx",
  "context": "region",
  "variants": {
    "invalid": {
      "prop": "invalid",
      "values": [
        "true"
      ]
    },
    "surface": {
      "prop": "surface",
      "values": [
        "base",
        "muted"
      ]
    },
    "size": {
      "prop": "size",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    }
  },
  "uses": [
    "Calendar",
    "Card",
    "Field",
    "Icon",
    "Layout",
    "Popover"
  ],
  "example": "const [stay, setStay] = useState<DateRange | undefined>()\nconst [reporting, setReporting] = useState<DateRange | undefined>()\n\n<Stack gap={6}>\n  <Field label=\"Stay dates\" htmlFor=\"stay\">\n    <DateRangePicker\n      label=\"Stay dates\"\n      surface=\"muted\"\n      placeholder=\"Select dates\"\n      value={stay}\n      onChange={setStay}\n    />\n  </Field>\n  <Card>\n    <Field label=\"Reporting period\" htmlFor=\"reporting\">\n      <DateRangePicker\n        label=\"Reporting period\"\n        placeholder=\"Select a period\"\n        value={reporting}\n        onChange={setReporting}\n      />\n    </Field>\n  </Card>\n</Stack>"
}
