{
  "ref": "Input",
  "level": "atom",
  "description": "The single-line text field: label, invalid state and the shared control tokens. A screen never renders a bare input.",
  "exports": [
    "Input"
  ],
  "main": "Input",
  "from": "@/components/Input",
  "slots": [],
  "props": [
    {
      "name": "invalid",
      "type": "boolean",
      "required": false,
      "description": "The field failed validation. A STATE, not a style: it turns the border and gives `<Field>` the hook that ties the error message to this control, so the reason is read out as part of the field. Colouring a border red by hand produces the look without the announcement."
    },
    {
      "name": "surface",
      "type": "'base' | 'muted'",
      "required": false,
      "description": "Which surface the field sits on. On `muted` (a page/PageHeader) the border is dropped since the white fill separates it; `base` (default, a white card or form) keeps the border. Invalid + focus stay visible on both.",
      "values": [
        "base",
        "muted"
      ]
    },
    {
      "name": "size",
      "type": "'sm' | 'md' | 'lg'",
      "required": false,
      "description": "Control height: sm / md (default) / lg — matches the button/control scale.",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    }
  ],
  "sourcePath": "src/components/Input/Input.tsx",
  "context": "region",
  "inherits": "Omit<ComponentPropsWithRef<'input'>, 'size'>",
  "variants": {
    "size": {
      "prop": "size",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    "surface": {
      "prop": "surface",
      "values": [
        "base",
        "muted"
      ]
    }
  },
  "uses": [
    "Field",
    "Layout",
    "Textarea"
  ],
  "example": "<Stack gap={4}>\n  <Field label=\"Document title\" htmlFor=\"title\" hint=\"Shown in the list and in search.\">\n    <Input id=\"title\" defaultValue=\"Q3 onboarding guide\" />\n  </Field>\n  <Field label=\"Reply-to address\" htmlFor=\"mail\" error=\"That does not look like an address.\">\n    <Input id=\"mail\" invalid defaultValue=\"not-an-address\" />\n  </Field>\n  <Input aria-label=\"Search documents\" placeholder=\"Search documents\" surface=\"muted\" size=\"sm\" />\n</Stack>"
}
