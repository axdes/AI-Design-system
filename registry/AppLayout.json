{
  "ref": "AppLayout",
  "level": "organism",
  "description": "The application shell: navigation and a scrolling main, in one of four arrangements. Routing-agnostic — pass any nav in the `nav` slot. Below the drawer breakpoint a side arrangement becomes an overlay drawer toggled by `navOpen`; `top` and `none` have nothing to drawer and stay as they are.",
  "exports": [
    "AppLayout"
  ],
  "main": "AppLayout",
  "from": "@/components/AppLayout",
  "slots": [],
  "props": [
    {
      "name": "nav",
      "type": "ReactNode",
      "required": false,
      "description": "The navigation (e.g. a <SideNav/>). Ignored when `layout` is \"none\"."
    },
    {
      "name": "children",
      "type": "ReactNode",
      "required": true
    },
    {
      "name": "arrangement",
      "type": "Arrangement",
      "required": false,
      "description": "How the shell is arranged: where the navigation sits, and whether it is attached to the content or detached from it.",
      "values": [
        "rail",
        "float",
        "top",
        "none"
      ]
    },
    {
      "name": "navOpen",
      "type": "boolean",
      "required": false,
      "description": "Mobile only: whether the nav drawer is open."
    },
    {
      "name": "onNavClose",
      "type": "() => void",
      "required": false,
      "description": "Mobile only: called when the backdrop is clicked."
    }
  ],
  "sourcePath": "src/components/AppLayout/AppLayout.tsx",
  "context": "page",
  "variants": {
    "arrangement": {
      "prop": "arrangement",
      "values": [
        "rail",
        "float",
        "top",
        "none"
      ]
    }
  },
  "uses": [
    "Card",
    "Icon",
    "Layout",
    "PageHeader",
    "Prose",
    "SideNav"
  ],
  "example": "const [navOpen, setNavOpen] = useState(false)\n/* The app frame: a nav rail plus the scrolling main column. On narrow screens\n * the rail becomes an overlay, which is what navOpen/onNavClose drive.\n *\n * The main column carries a REAL page — a header and content under it. It\n * used to hold a bare <PageHeader> and nothing else, so the one thing this\n * component exists to arrange (the relationship between the rail and what is\n * beside it) was an empty grey field (owner: \"AppLayout is wrong\",\n * 2026-08-24). What goes in `children` is always a page; showing one is what\n * makes the arrangement legible. */\n\n<AppLayout\n  navOpen={navOpen}\n  onNavClose={() => setNavOpen(false)}\n  nav={\n    <SideNav\n      aria-label=\"Primary\"\n      logo={<><Icon name=\"auto_awesome\" size=\"md\" /><strong>Acme</strong></>}\n      logoMark={<Icon name=\"auto_awesome\" size=\"md\" />}\n      groups={[\n        {\n          label: 'Workspace',\n          items: [\n            { id: 'library', label: 'Library', icon: 'folder', active: true },\n            { id: 'review', label: 'For review', icon: 'check_circle' },\n          ],\n        },\n      ]}\n    />\n  }\n>\n  <PageHeader title=\"Content library\" />\n  <Stack gap={4}>\n   "
}
