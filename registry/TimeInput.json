{
  "ref": "TimeInput",
  "level": "atom",
  "description": "A time-of-day field: the native <input type=\"time\"> in the system's Input clothes, so the picker, the validation and the keyboard stay the browser's. The value is always 24-hour \"HH:MM\"; the browser shows it in the user's own clock convention. Wrap it in <Field> for its label, like <Input>.",
  "exports": [
    "TimeInput"
  ],
  "main": "TimeInput",
  "from": "@/components/TimeInput",
  "slots": [],
  "props": [
    {
      "name": "value",
      "type": "string",
      "required": false,
      "description": "24-hour \"HH:MM\" (the native time value). Empty string = not set yet."
    },
    {
      "name": "onChange",
      "type": "(value: string) => void",
      "required": false
    },
    {
      "name": "invalid",
      "type": "boolean",
      "required": false
    },
    {
      "name": "surface",
      "type": "'base' | 'muted'",
      "required": false,
      "description": "Which surface the field sits on; same contract as <Input>.",
      "values": [
        "base",
        "muted"
      ]
    },
    {
      "name": "size",
      "type": "'sm' | 'md' | 'lg'",
      "required": false,
      "description": "Control height: sm / md (default) / lg — matches the button/control scale.",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    }
  ],
  "sourcePath": "src/components/TimeInput/TimeInput.tsx",
  "inherits": "Omit<ComponentPropsWithRef<'input'>, 'size' | 'type' | 'value' | 'onChange'>",
  "variants": {
    "size": {
      "prop": "size",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    "surface": {
      "prop": "surface",
      "values": [
        "base",
        "muted"
      ]
    }
  },
  "uses": [
    "Card",
    "Field",
    "Layout"
  ],
  "example": "const [start, setStart] = useState('09:30')\nconst [end, setEnd] = useState('10:15')\nconst [reminder, setReminder] = useState('08:45')\n\n<Stack gap={6}>\n  <Field label=\"Meeting time\" htmlFor=\"starts\">\n    <Row gap={2} align=\"center\" nowrap>\n      <TimeInput id=\"starts\" aria-label=\"Starts at\" surface=\"muted\" value={start} onChange={setStart} />\n      <span>to</span>\n      <TimeInput aria-label=\"Ends at\" surface=\"muted\" value={end} onChange={setEnd} />\n    </Row>\n  </Field>\n  <Card>\n    <Field label=\"Send the reminder at\" htmlFor=\"reminder\">\n      <TimeInput id=\"reminder\" aria-label=\"Send the reminder at\" value={reminder} onChange={setReminder} />\n    </Field>\n  </Card>\n</Stack>"
}
