{
  "ref": "IconButton",
  "level": "atom",
  "description": "A button whose label is an icon, which is why it always takes an aria-label and a Tooltip. `quiet` for inline toolbars, `filled` plus `tone` for send and stop. Copy: the aria-label is the same verb a worded button would carry, and it names the target when a screen repeats the control: \"Delete invoice INV-1041\", not \"Delete\".",
  "exports": [
    "IconButton"
  ],
  "main": "IconButton",
  "from": "@/components/IconButton",
  "slots": [],
  "props": [
    {
      "name": "ref",
      "type": "Ref<HTMLButtonElement>",
      "required": false,
      "description": "Forwarded to the underlying <button>."
    },
    {
      "name": "icon",
      "type": "IconName",
      "required": true
    },
    {
      "name": "size",
      "type": "Size",
      "required": false,
      "description": "Square button matching control heights: sm 32 / md 40 / lg 52. Default 'sm'. NOTE: `Button` defaults to 'md', so a pair of them needs this stated — an unsized icon button beside an unsized button is 32 next to 40. Three apps shipped that before `controls-same-height` started asking.",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    {
      "name": "variant",
      "type": "Variant",
      "required": false,
      "description": "ghost = transparent (default, hover = accent fill). filled = solid fill. quiet = transparent, hover recolors only (no fill) — for inline toolbar icons.",
      "values": [
        "ghost",
        "filled",
        "quiet"
      ]
    },
    {
      "name": "tone",
      "type": "Tone",
      "required": false,
      "description": "Fill colour for `filled` (e.g. send = primary, stop = destructive).",
      "values": [
        "primary",
        "destructive"
      ]
    },
    {
      "name": "reveal",
      "type": "boolean",
      "required": false,
      "description": "Hidden by default; container's :hover or `data-open` makes it visible."
    },
    {
      "name": "loading",
      "type": "boolean",
      "required": false,
      "description": "In-flight action: swaps the icon for a spinner and blocks clicks. The spinner reuses the button's aria-label as its accessible name."
    }
  ],
  "sourcePath": "src/components/IconButton/IconButton.tsx",
  "inherits": "Omit<ButtonHTMLAttributes<HTMLButtonElement>, 'children'>",
  "variants": {
    "size": {
      "prop": "size",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    "variant": {
      "prop": "variant",
      "values": [
        "ghost",
        "filled",
        "quiet"
      ]
    },
    "tone": {
      "prop": "tone",
      "values": [
        "primary",
        "destructive"
      ]
    },
    "reveal": {
      "prop": "reveal",
      "values": [
        "true"
      ]
    },
    "loading": {
      "prop": "loading",
      "values": [
        "true"
      ]
    }
  },
  "uses": [
    "Icon",
    "Spinner",
    "Tooltip"
  ],
  "example": "/* Icon-only controls carry BOTH an aria-label (the accessible name) and a\n * Tooltip (the visible name on hover/focus). Neither is optional. */\n\n<Tooltip content=\"Delete\">\n  <IconButton icon=\"delete\" tone=\"destructive\" aria-label=\"Delete\" onClick={() => undefined} />\n</Tooltip>"
}
