{
  "ref": "SideNav",
  "level": "organism",
  "description": "The application navigation column: sections, the active route, and the collapse to an icon rail.",
  "exports": [
    "SideNav"
  ],
  "main": "SideNav",
  "from": "@/components/SideNav",
  "slots": [],
  "props": [
    {
      "name": "groups",
      "type": "SideNavGroup[]",
      "required": true
    },
    {
      "name": "logo",
      "type": "ReactNode",
      "required": false,
      "description": "Full brand lockup, shown when expanded (mark + wordmark)."
    },
    {
      "name": "logoMark",
      "type": "ReactNode",
      "required": false,
      "description": "The mark alone, shown when collapsed. Provide it whenever the rail can collapse: the fallback is `logo`, and a wordmark does not fit the rail."
    },
    {
      "name": "footer",
      "type": "ReactNode",
      "required": false,
      "description": "Pinned to the bottom (e.g. a user menu)."
    },
    {
      "name": "collapsible",
      "type": "boolean",
      "required": false,
      "description": "Master switch for the collapse affordance (default true)."
    },
    {
      "name": "collapseControl",
      "type": "CollapseControl",
      "required": false,
      "description": "Which control collapses the rail. `logo` is the default and is the decided answer: collapsed, the biggest target on the rail is what opens the rail, and expanded, hovering the mark says what pressing it will do. It used to default to `bottom`, and the measurement on 2026-08-23 was that no product passed this prop at all — so every one of them carried a Collapse button nobody had chosen, while the behaviour they wanted sat behind a default.",
      "values": [
        "bottom",
        "logo",
        "both"
      ]
    },
    {
      "name": "collapsed",
      "type": "boolean",
      "required": false,
      "description": "Controlled collapsed state. Omit to let the component manage it."
    },
    {
      "name": "defaultCollapsed",
      "type": "boolean",
      "required": false,
      "description": "The state on first render when the rail owns its own; ignored once `collapsed` is passed. Start collapsed only where the product is a workspace people live in rather than navigate."
    },
    {
      "name": "onCollapsedChange",
      "type": "(collapsed: boolean) => void",
      "required": false,
      "description": "Called with the next collapsed state, for a product that persists the rail across sessions. Omit it and the rail keeps its own state."
    },
    {
      "name": "aria-label",
      "type": "string",
      "required": false
    }
  ],
  "sourcePath": "src/components/SideNav/SideNav.tsx",
  "context": "page",
  "variants": {
    "collapsed": {
      "prop": "collapsed",
      "values": [
        "true"
      ]
    },
    "has-mark": {
      "prop": "logoMark",
      "values": null
    }
  },
  "uses": [
    "Badge",
    "Icon",
    "Layout"
  ],
  "example": "const [active, setActive] = useState('library')\nconst [homeActive, setHomeActive] = useState('library')\nconst workspace = (current: string, set: (id: string) => void) => [\n  {\n    label: 'Workspace',\n    items: [\n      { id: 'library', label: 'Library', icon: 'folder' as const, active: current === 'library', onSelect: () => set('library') },\n      { id: 'review', label: 'For review', icon: 'check_circle' as const, active: current === 'review', trailing: <Badge tone=\"warning\">3</Badge>, usage: 'For review', onSelect: () => set('review') },\n    ],\n  },\n  {\n    label: 'Admin',\n    items: [{ id: 'users', label: 'Users', icon: 'group' as const, active: current === 'users', onSelect: () => set('users') }],\n  },\n]\n\n<Row gap={8} align=\"start\">\n  <SideNav\n    aria-label=\"Primary\"\n    logo={<>{BRAND}<strong>Acme</strong></>}\n    logoMark={BRAND}\n    groups={workspace(active, setActive)}\n  />\n  \n  <SideNav\n    aria-label=\"Primary, with a home link\"\n    collapseControl=\"bottom\"\n    logo={<a href=\"#top\">{BRAND}<strong>Acme</strong></a>}\n    logoMark={<a href=\"#top\">{BRAND}</a>}\n    groups={workspace(homeActive, setHomeActive)}\n  />\n</Row>"
}
