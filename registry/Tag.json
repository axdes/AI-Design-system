{
  "ref": "Tag",
  "level": "molecule",
  "description": "A static, non-interactive label token in a pill — the read-only counterpart to <Chip> (which is a button). Optionally removable: the label does nothing, only the trailing X drops the token, so the remove control can be its own button (a remove button nested inside a Chip button would be invalid HTML). Copy: the value as the reader wrote or chose it, unaltered. A tag is data; rewording it makes it un-findable.",
  "exports": [
    "Tag"
  ],
  "main": "Tag",
  "from": "@/components/Tag",
  "slots": [],
  "props": [
    {
      "name": "children",
      "type": "ReactNode",
      "required": true
    },
    {
      "name": "onRemove",
      "type": "() => void",
      "required": false,
      "description": "When set, renders a trailing remove button that calls this on click."
    },
    {
      "name": "removeLabel",
      "type": "string",
      "required": false,
      "description": "Accessible label + tooltip for the remove button (required when removable)."
    },
    {
      "name": "size",
      "type": "'sm' | 'md'",
      "required": false,
      "values": [
        "sm",
        "md"
      ]
    },
    {
      "name": "className",
      "type": "string",
      "required": false
    }
  ],
  "sourcePath": "src/components/Tag/Tag.tsx",
  "variants": {
    "size": {
      "prop": "size",
      "values": [
        "sm",
        "md"
      ]
    }
  },
  "uses": [
    "Icon",
    "Layout",
    "Tooltip"
  ],
  "example": "const [recipients, setRecipients] = useState(['Sarah', 'Ahmed'])\n/* Tag is the read-only counterpart to Chip: a selection token, not a button.\n * Use Chip when the pill itself is the control. */\n\n<Row gap={2}>\n  {recipients.map((name) => (\n    <Tag key={name} onRemove={() => setRecipients((r) => r.filter((x) => x !== name))} removeLabel={`Remove ${name}`}>\n      {name}\n    </Tag>\n  ))}\n</Row>"
}
