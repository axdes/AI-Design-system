{
  "ref": "PasswordInput",
  "level": "molecule",
  "description": "A password field that can be read back. Every login checklist asks for this and no field in this system had it, so ten of them shipped as a bare `type=\"password\"`: a typo in a masked field is invisible, and the only recovery is to clear it and start again. The toggle is a button rather than a checkbox because it does not change what will be submitted, only what is on screen.",
  "exports": [
    "PasswordInput"
  ],
  "main": "PasswordInput",
  "from": "@/components/PasswordInput",
  "slots": [],
  "props": [
    {
      "name": "invalid",
      "type": "boolean",
      "required": false,
      "description": "A STATE, not a style: it turns the border and hands <Field> the hook it needs to read the error out as part of the field."
    },
    {
      "name": "surface",
      "type": "'base' | 'muted'",
      "required": false,
      "description": "Names what is BEHIND the field. On a --muted ground the white fill already separates it, so the resting border comes off; on a card the surface is white too, and `base` keeps the border that does the separating.",
      "values": [
        "base",
        "muted"
      ]
    },
    {
      "name": "size",
      "type": "'sm' | 'md' | 'lg'",
      "required": false,
      "description": "Follows the form it sits in; the reveal button follows it too.",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    }
  ],
  "sourcePath": "src/components/PasswordInput/PasswordInput.tsx",
  "context": "region",
  "inherits": "Omit<ComponentPropsWithRef<'input'>, 'size' | 'type'>",
  "variants": {
    "size": {
      "prop": "size",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    }
  },
  "uses": [
    "Card",
    "Field",
    "IconButton",
    "Input",
    "Layout",
    "Tooltip"
  ],
  "example": "const [created, setCreated] = useState('')\nconst [current, setCurrent] = useState('')\n\n<Stack gap={6}>\n  \n  <Field label=\"Choose a password\" htmlFor=\"new-password\" hint=\"At least 12 characters\">\n    <PasswordInput\n      id=\"new-password\"\n      surface=\"muted\"\n      value={created}\n      autoComplete=\"new-password\"\n      onChange={(e) => { setCreated(e.target.value) }}\n    />\n  </Field>\n  \n  <Card>\n    <Field label=\"Confirm with your current password\" htmlFor=\"current-password\">\n      <PasswordInput\n        id=\"current-password\"\n        value={current}\n        autoComplete=\"new-password\"\n        onChange={(e) => { setCurrent(e.target.value) }}\n      />\n    </Field>\n  </Card>\n</Stack>"
}
