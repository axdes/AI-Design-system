{
  "ref": "ConditionalReveal",
  "level": "molecule",
  "description": "The dependent field under an option: present only while that option is chosen, indented under it so the two read as one question.",
  "exports": [
    "ConditionalReveal"
  ],
  "main": "ConditionalReveal",
  "from": "@/components/ConditionalReveal",
  "slots": [],
  "props": [
    {
      "name": "when",
      "type": "boolean",
      "required": true,
      "description": "True when the option this content belongs to is the chosen one."
    },
    {
      "name": "children",
      "type": "ReactNode",
      "required": true
    },
    {
      "name": "className",
      "type": "string",
      "required": false
    }
  ],
  "sourcePath": "src/components/ConditionalReveal/ConditionalReveal.tsx",
  "uses": [
    "Field",
    "Input",
    "Layout",
    "Radio"
  ],
  "example": "const [reason, setReason] = useState('scope')\nconst [other, setOther] = useState('')<Stack gap={2} role=\"radiogroup\" aria-label=\"Why is the session moving?\">\n  <Radio\n    name=\"reason\"\n    value=\"scope\"\n    label=\"The scope changed\"\n    checked={reason === 'scope'}\n    onChange={() => setReason('scope')}\n  />\n  <Radio\n    name=\"reason\"\n    value=\"other\"\n    label=\"Something else\"\n    checked={reason === 'other'}\n    onChange={() => setReason('other')}\n  />\n  <ConditionalReveal when={reason === 'other'}>\n    <Field label=\"What happened?\" htmlFor=\"reason-other\">\n      <Input id=\"reason-other\" value={other} onChange={(e) => setOther(e.target.value)} />\n    </Field>\n  </ConditionalReveal>\n</Stack>"
}
