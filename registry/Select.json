{
  "ref": "Select",
  "level": "molecule",
  "description": "Single choice from a short list, built on Dropdown so the menu can match the trigger width. Combobox is the one for a long list. Copy: the label names the value, the placeholder names the choice not yet made — \"Choose a region\". Options are parallel and never restate the label.",
  "exports": [
    "Select"
  ],
  "main": "Select",
  "from": "@/components/Select",
  "slots": [],
  "props": [
    {
      "name": "value",
      "type": "V | undefined",
      "required": true
    },
    {
      "name": "onChange",
      "type": "(next: V) => void",
      "required": true
    },
    {
      "name": "options",
      "type": "readonly SelectOption<V>[]",
      "required": true
    },
    {
      "name": "placeholder",
      "type": "string",
      "required": false
    },
    {
      "name": "tabIndex",
      "type": "number",
      "required": false,
      "description": "For a select inside a ROVING TABINDEX container — a grid cell, a toolbar — where exactly one of many controls is in the tab order at a time. Left alone the trigger is an ordinary tab stop, which is right everywhere else."
    },
    {
      "name": "label",
      "type": "string",
      "required": true,
      "description": "Accessible label — applied to the trigger button."
    },
    {
      "name": "id",
      "type": "string",
      "required": false,
      "description": "Lands on the trigger, so `<Field htmlFor>` reaches it and an `ErrorSummary` row can put focus on it. Without one the field is unreachable by name: a failed submit listed \"Choose who takes this request\", the row linked to an id nothing carried, and clicking it did nothing at all (owner, 2026-08-23)."
    },
    {
      "name": "className",
      "type": "string",
      "required": false
    },
    {
      "name": "size",
      "type": "'sm' | 'md' | 'lg'",
      "required": false,
      "description": "sm / md (default) / lg — matches the control scale.",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    {
      "name": "disabled",
      "type": "boolean",
      "required": false,
      "description": "Not editable and not openable."
    },
    {
      "name": "invalid",
      "type": "boolean",
      "required": false,
      "description": "Failed validation: paints the red border. It does NOT announce anything, and the description here used to claim `aria-invalid`, which the component never set — and could not: the trigger is a `<button>`, and `aria-invalid` is not supported on that role. Nor is the widget a real ARIA combobox: its popup is a `menu` of `menuitem`s, so calling the trigger a combobox would be a different lie. Until a listbox-based select exists, the error TEXT is what carries the meaning, and the caller has to associate it — `<Field>` has no error slot yet, which is the actual gap."
    },
    {
      "name": "surface",
      "type": "'base' | 'muted'",
      "required": false,
      "description": "Which surface the field sits on. On `muted` (a page/PageHeader) the border is dropped since the white fill separates it; `base` (default, a white card or form) keeps the border. Invalid + focus stay visible on both.",
      "values": [
        "base",
        "muted"
      ]
    }
  ],
  "sourcePath": "src/components/Select/Select.tsx",
  "context": "region",
  "variants": {
    "size": {
      "prop": "size",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    "invalid": {
      "prop": "invalid",
      "values": [
        "true"
      ]
    },
    "surface": {
      "prop": "surface",
      "values": [
        "base",
        "muted"
      ]
    }
  },
  "uses": [
    "Dropdown",
    "Icon",
    "Layout"
  ],
  "example": "const [type, setType] = useState<Leave>('annual')\nconst [second, setSecond] = useState<Leave>('sick')\n\n<Row gap={3} align=\"end\">\n  <Select<Leave> label=\"Leave type\" value={type} onChange={setType} options={LEAVE} />\n  <Select<Leave> label=\"In a toolbar\" surface=\"muted\" size=\"sm\" value={second} onChange={setSecond} options={LEAVE} />\n</Row>"
}
