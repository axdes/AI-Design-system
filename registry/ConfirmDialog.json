{
  "ref": "ConfirmDialog",
  "level": "block",
  "description": "\"Are you sure?\" — the yes/no dialog, as opposed to `<FormModal>` which collects input. `destructive` colours the confirm so a delete never looks like a save.",
  "exports": [
    "ConfirmDialog"
  ],
  "main": "ConfirmDialog",
  "from": "@/blocks/ConfirmDialog",
  "slots": [],
  "props": [
    {
      "name": "open",
      "type": "boolean",
      "required": true
    },
    {
      "name": "title",
      "type": "string",
      "required": true
    },
    {
      "name": "message",
      "type": "ReactNode",
      "required": true
    },
    {
      "name": "confirmLabel",
      "type": "ReactNode",
      "required": false
    },
    {
      "name": "cancelLabel",
      "type": "ReactNode",
      "required": false
    },
    {
      "name": "confirmVariant",
      "type": "'primary' | 'destructive'",
      "required": false,
      "values": [
        "primary",
        "destructive"
      ]
    },
    {
      "name": "onConfirm",
      "type": "() => void",
      "required": true
    },
    {
      "name": "onClose",
      "type": "() => void",
      "required": true
    },
    {
      "name": "busy",
      "type": "boolean",
      "required": false
    }
  ],
  "sourcePath": "src/blocks/ConfirmDialog/ConfirmDialog.tsx",
  "context": "region",
  "uses": [
    "Button",
    "Modal"
  ],
  "example": "/* Open by default so the example shows the dialog itself, the way Modal and\n * FormModal do: a closed dialog documents nothing and leaves the visual\n * baseline watching a button. */\nconst [open, setOpen] = useState(true)\nconst [busy, setBusy] = useState(false)\n/* A confirm exists to slow down an irreversible action, so the destructive\n * variant and a message that names the consequence are the point, not decoration. */\nconst remove = () => {\n  setBusy(true)\n  setTimeout(() => {\n    setBusy(false)\n    setOpen(false)\n  }, 600)\n}\n\n<>\n  <Button variant=\"destructive\" onClick={() => setOpen(true)}>\n    Delete workshop\n  </Button>\n  <ConfirmDialog\n    open={open}\n    title=\"Delete this workshop?\"\n    message=\"The transcript and every card generated from it are removed. This cannot be undone.\"\n    confirmLabel=\"Delete\"\n    confirmVariant=\"destructive\"\n    busy={busy}\n    onConfirm={remove}\n    onClose={() => setOpen(false)}\n  />\n</>"
}
