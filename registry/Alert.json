{
  "ref": "Alert",
  "level": "molecule",
  "description": "Inline message on a region: `tone` carries the meaning and `onDismiss` makes it dismissible. Default role is status; pass role=\"alert\" for an error. Dismiss info and success, never an unresolved error. Copy: one sentence about what happened, in the reader's terms, not the system's. A tone is not a substitute for words: \"Saved\" and \"Could not save — the file is open elsewhere\" both need saying.",
  "exports": [
    "Alert"
  ],
  "main": "Alert",
  "from": "@/components/Alert",
  "slots": [],
  "props": [
    {
      "name": "tone",
      "type": "Tone",
      "required": false,
      "description": "What KIND of news it is, and it decides whether the reader must act: `danger` something is broken now, `warning` something will break, `success` it worked, `info` context they did not ask for, `neutral` a note. Everything toned danger is nothing toned danger.",
      "values": [
        "neutral",
        "danger",
        "warning",
        "success",
        "info"
      ]
    },
    {
      "name": "icon",
      "type": "IconName",
      "required": false,
      "description": "Override the tone icon (e.g. `sparkles` for AI-assist messages)."
    },
    {
      "name": "compact",
      "type": "boolean",
      "required": false,
      "description": "Dense one-line variant: small padding, centred icon."
    },
    {
      "name": "action",
      "type": "ReactNode",
      "required": false,
      "description": "Trailing action (e.g. a Button), pinned to the end of the alert row."
    },
    {
      "name": "onDismiss",
      "type": "() => void",
      "required": false
    }
  ],
  "sourcePath": "src/components/Alert/Alert.tsx",
  "context": "region",
  "inherits": "HTMLAttributes<HTMLDivElement>",
  "variants": {
    "tone": {
      "prop": "tone",
      "values": [
        "neutral",
        "danger",
        "warning",
        "success",
        "info"
      ]
    },
    "compact": {
      "prop": "compact",
      "values": [
        "true"
      ]
    }
  },
  "uses": [
    "Button",
    "Icon",
    "IconButton",
    "Layout",
    "Tooltip"
  ],
  "example": "const [showTip, setShowTip] = useState(true)\n\n<Stack gap={3}>\n  \n  <Alert tone=\"danger\" role=\"alert\">Couldn&apos;t save. Check your connection.</Alert>\n  \n  {showTip && <Alert tone=\"info\" onDismiss={() => setShowTip(false)}>Drafts save automatically.</Alert>}\n  \n  <Alert tone=\"warning\" action={<Button variant=\"secondary\" size=\"sm\">Reconnect</Button>}>\n    The editor lost the connection two minutes ago.\n  </Alert>\n</Stack>"
}
