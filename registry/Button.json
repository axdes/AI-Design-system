{
  "ref": "Button",
  "level": "atom",
  "description": "The action control: `variant` sets its weight, `loading` swaps the icon for a spinner and disables it (`iconEnd` is a deprecated no-op: icons always trail) actions. Copy: the verb the reader is about to perform, with its object when the screen has more than one — \"Send invoice\", not \"Submit\", never \"OK\". The label is a promise about what happens next.",
  "exports": [
    "Button"
  ],
  "main": "Button",
  "from": "@/components/Button",
  "slots": [],
  "props": [
    {
      "name": "ref",
      "type": "Ref<HTMLButtonElement>",
      "required": false,
      "description": "Forwarded to the underlying <button>."
    },
    {
      "name": "variant",
      "type": "Variant",
      "required": false,
      "description": "How much weight this action carries ON THE SCREEN — which makes it a decision about the screen, not about the button. Exactly one action is `primary`: the thing the reader came to do. Everything beside it is `secondary`, which is also what a button with no variant gets, and that is the right default for a control nobody has thought about yet. `ghost` is for a control that must not compete with the content under it. `destructive` is reserved for what cannot be undone, where the colour IS the warning — and it does not remove the need to confirm.",
      "values": [
        "primary",
        "secondary",
        "ghost",
        "destructive",
        "success",
        "dark",
        "link",
        "ai"
      ]
    },
    {
      "name": "size",
      "type": "Size",
      "required": false,
      "description": "Control height: sm 32 / md 40 (default) / lg 52. State it whenever the button sits beside an `<IconButton>`, which defaults to `sm`.",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    {
      "name": "block",
      "type": "boolean",
      "required": false,
      "description": "Fills the width it is given. For a button that is the whole of a narrow column — a form's submit on a phone, a card's single action — never as a way to make one button look important in a row."
    },
    {
      "name": "iconEnd",
      "type": "boolean",
      "required": false,
      "description": "@deprecated Inert. Every icon in a button trails the label from one line of CSS (`.btn > .icon { order: 1 }`, owner 2026-06-10), and nothing reads this. It survived the rule it belonged to. Do not pass it in new code."
    },
    {
      "name": "loading",
      "type": "boolean",
      "required": false,
      "description": "In-flight action: shows a spinner in place of the icon and blocks further clicks. The label stays so the button keeps its width. Set the accessible busy text with `loadingLabel`."
    },
    {
      "name": "loadingLabel",
      "type": "string",
      "required": false,
      "description": "What the spinner announces while `loading` (default \"Loading\")."
    },
    {
      "name": "children",
      "type": "ReactNode",
      "required": false
    }
  ],
  "sourcePath": "src/components/Button/Button.tsx",
  "inherits": "ButtonHTMLAttributes<HTMLButtonElement>",
  "variants": {
    "variant": {
      "prop": "variant",
      "values": [
        "primary",
        "secondary",
        "ghost",
        "destructive",
        "success",
        "dark",
        "link",
        "ai"
      ]
    },
    "size": {
      "prop": "size",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    "block": {
      "prop": "block",
      "values": [
        "true"
      ]
    },
    "icon-end": {
      "prop": "iconEnd",
      "values": [
        "true"
      ]
    },
    "loading": {
      "prop": "loading",
      "values": [
        "true"
      ]
    }
  },
  "uses": [
    "Icon",
    "IconButton",
    "Layout",
    "Spinner"
  ],
  "example": "<Row gap={2} align=\"center\">\n  <Button variant=\"primary\">\n    Create document\n    <Icon name=\"add\" />\n  </Button>\n  <Button>Save draft</Button>\n  <Button variant=\"ghost\">Cancel</Button>\n  <Button variant=\"destructive\" loading loadingLabel=\"Deleting\">Delete</Button>\n</Row>"
}
