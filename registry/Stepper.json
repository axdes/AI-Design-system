{
  "ref": "Stepper",
  "level": "molecule",
  "description": "Progress across an ordered set of steps (a wizard, a multi-step form). Shows which are done (check), which is active (aria-current=\"step\") and which are still ahead. Presentational: the parent owns `current` and what each step contains. Completed steps can be clickable to go back. Copy: step names are nouns, short and parallel, and they stay the same word in the step and in its page title.",
  "exports": [
    "Stepper"
  ],
  "main": "Stepper",
  "from": "@/components/Stepper",
  "slots": [],
  "props": [
    {
      "name": "steps",
      "type": "Step[]",
      "required": true
    },
    {
      "name": "current",
      "type": "number",
      "required": true,
      "description": "0-based index of the step in progress. Earlier steps read as complete."
    },
    {
      "name": "onSelect",
      "type": "(index: number) => void",
      "required": false,
      "description": "Lets the user jump back to a completed step. Only completed steps are interactive; the current and future ones are not."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "description": "Accessible name for the ordered list. Default \"Progress\"."
    },
    {
      "name": "className",
      "type": "string",
      "required": false
    }
  ],
  "sourcePath": "src/components/Stepper/Stepper.tsx",
  "context": "region",
  "uses": [
    "Icon"
  ],
  "example": "const [step, setStep] = useState(1)<Stepper\n  current={step}\n  onSelect={setStep}\n  steps={[\n    { label: 'Account', description: 'Your details' },\n    { label: 'Shipping', description: 'Where to send it' },\n    { label: 'Payment', description: 'How to pay' },\n    { label: 'Review', description: 'Confirm and place' },\n  ]}\n/>"
}
