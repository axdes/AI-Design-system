{
  "ref": "SettingsPageTemplate",
  "level": "block",
  "description": "The SETTINGS page skeleton: a deliberately narrow column of sections, each a Card of explained rows. Found by scanning, so the words carry it: group by who a knob affects, and every row says what it actually changes. Copy: the title is the area, not the word \"Settings\" twice over. Each row's label is the setting as the reader would ask for it, and its description says what changes when it does.",
  "exports": [
    "SettingsPageTemplate",
    "SettingsSection",
    "SettingRow"
  ],
  "main": "SettingsPageTemplate",
  "from": "@/blocks/SettingsPageTemplate",
  "slots": [
    {
      "name": "SettingsSection"
    },
    {
      "name": "SettingRow"
    }
  ],
  "props": [
    {
      "name": "title",
      "type": "ReactNode",
      "required": false,
      "description": "Page title in the header."
    },
    {
      "name": "actions",
      "type": "ReactNode",
      "required": false,
      "description": "Header actions — typically nothing: settings save per control or per row."
    },
    {
      "name": "children",
      "type": "ReactNode",
      "required": false,
      "description": "The `<SettingsSection>` stack."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Extra class on the page wrapper."
    }
  ],
  "sourcePath": "src/blocks/SettingsPageTemplate/SettingsPageTemplate.tsx",
  "context": "page",
  "uses": [
    "Badge",
    "Button",
    "Card",
    "Modal",
    "PasswordInput",
    "Select",
    "Switch"
  ],
  "example": "const [language, setLanguage] = useState('en')\nconst [digest, setDigest] = useState(true)\nconst [confirming, setConfirming] = useState(false)\n\n<SettingsPageTemplate title=\"Settings\">\n  <SettingsSection\n    title=\"Preferences\"\n    description=\"How this workspace looks and speaks, for you only.\"\n  >\n    <SettingRow\n      label=\"Language\"\n      description=\"The interface language. Documents keep the language they were written in.\"\n    >\n      \n      <Select\n        label=\"Language\"\n        value={language}\n        onChange={setLanguage}\n        options={[\n          { value: 'en', label: 'English' },\n          { value: 'ar', label: 'العربية' },\n        ]}\n      />\n    </SettingRow>\n    <SettingRow\n      label=\"Weekly digest\"\n      description=\"One summary mail on Monday morning. Applies the moment it is flipped.\"\n    >\n      <Switch checked={digest} onChange={setDigest} label=\"Weekly digest\" />\n    </SettingRow>\n  </SettingsSection>\n  <SettingsSection\n    title=\"Connections\"\n    description=\"Keys this workspace uses on your behalf. Values are write-only: paste to replace, never read back.\">\n    <SettingRow\n      label=\"Transcription key\"\n      description=\"Used by the recorder. Stored "
}
