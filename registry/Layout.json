{
  "ref": "Layout",
  "level": "atom",
  "description": "Layout primitives: Stack and Row for gaps in one direction, Grid for a set of cards — auto-fitting equals by default, or `columnCount={12}` with `GridItem` spans when the set is deliberately unequal. `gap` is a token step, never a raw px.",
  "exports": [
    "Grid",
    "GridItem",
    "Row",
    "Stack"
  ],
  "main": "Grid",
  "from": "@/components/Layout",
  "slots": [
    {
      "name": "GridItem"
    },
    {
      "name": "Row"
    },
    {
      "name": "Stack"
    }
  ],
  "props": [
    {
      "name": "gap",
      "type": "1 | 2 | 3 | 4 | 6 | 8 | 12 | 16",
      "required": false,
      "description": "Space between the tracks, as a step of the 4pt scale — spacing lives on the container, never as margins on the children. The scale is the whole point: a number that is not a step is a decision nobody made.",
      "values": [
        1,
        2,
        3,
        4,
        6,
        8,
        12,
        16
      ]
    },
    {
      "name": "columnCount",
      "type": "12",
      "required": false,
      "description": "Twelve equal tracks instead of the auto-fitting default: the layout where items are DIFFERENT sizes (a wide feature beside two narrow tiles), each child wrapped in `GridItem` with its `span`. Omit it and every child gets the same width, which is the right answer for a set of equals.",
      "values": [
        12
      ]
    },
    {
      "name": "minColumn",
      "type": "'sm' | 'md' | 'lg'",
      "required": false,
      "description": "The narrowest a card in this grid may get before the grid drops a column. `sm` (16rem) is the default and right for tiles that carry a word or two; `md` (20rem) is what a card with a name, a role and an action needs; `lg` (24rem) is for a card with a picture and two sentences. Measured, not guessed: at `sm` a person card came out 250px wide on a laptop and every name broke over two lines.",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    {
      "name": "align",
      "type": "'start' | 'center' | 'end'",
      "required": false,
      "description": "How the children sit in their row. The default stretches them to the tallest, and for CARDS that default is the rule: cards in a row are always the same height (owner, 23.08). A ragged row is answered by giving the short card the part it is missing, never by `start`. Use these values for a grid of things that are not cards — captions, thumbnails, controls — where a row of unequal boxes is what was asked for.",
      "values": [
        "start",
        "center",
        "end"
      ]
    },
    {
      "name": "alignRows",
      "type": "boolean",
      "required": false,
      "description": "Makes every row as tall as its tallest item, so cards in a grid line up across rows instead of each row setting its own height. Leave it off when the items are meant to be their own size."
    }
  ],
  "sourcePath": "src/components/Layout/Grid.tsx",
  "inherits": "HTMLAttributes<HTMLDivElement>",
  "variants": {
    "gap": {
      "prop": "gap",
      "values": [
        1,
        2,
        3,
        4,
        6,
        8,
        12,
        16
      ]
    },
    "column-count": {
      "prop": "columnCount",
      "values": [
        12
      ]
    },
    "min-column": {
      "prop": "minColumn",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    "align": {
      "prop": "align",
      "values": [
        "start",
        "center",
        "end"
      ]
    },
    "align-rows": {
      "prop": "alignRows",
      "values": [
        "true"
      ]
    }
  },
  "uses": [
    "Button",
    "Card"
  ],
  "example": "const cards = ['Drafts', 'In review', 'Published']<Stack gap={6}>\n  \n  <Grid gap={4}>\n    {cards.map((c) => (\n      <Card key={c}><CardTitle>{c}</CardTitle></Card>\n    ))}\n  </Grid>\n  \n  <Grid gap={4} columnCount={12}>\n    <GridItem span={9}>\n      <Card stretch><CardTitle>This quarter</CardTitle></Card>\n    </GridItem>\n    <GridItem span={3}>\n      <Card stretch><CardTitle>Open items</CardTitle></Card>\n    </GridItem>\n  </Grid>\n  <Row gap={2}>\n    <Button variant=\"secondary\">Cancel</Button>\n    <Button variant=\"primary\">Save</Button>\n  </Row>\n</Stack>"
}
