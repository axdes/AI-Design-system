{
  "ref": "CountBadge",
  "level": "molecule",
  "description": "A count or dot pinned to the top-inline-end corner of whatever it wraps — notifications on a bell, unread on an avatar. Distinct from <Badge>, which is a standalone status pill. Hidden entirely when count is 0 and no dot. Copy: the accessible label counts the thing, not the badge — \"3 unread messages\". A number alone is read as \"three\" with nothing to attach it to.",
  "exports": [
    "CountBadge"
  ],
  "main": "CountBadge",
  "from": "@/components/CountBadge",
  "slots": [],
  "props": [
    {
      "name": "children",
      "type": "ReactNode",
      "required": true,
      "description": "The element the marker sits on (an Icon, IconButton, Avatar)."
    },
    {
      "name": "count",
      "type": "number",
      "required": false,
      "description": "Numeric count. Values over `max` render as `${max}+`. Omit for a plain dot."
    },
    {
      "name": "dot",
      "type": "boolean",
      "required": false,
      "description": "Show a small dot with no number (e.g. \"unread\", \"online\")."
    },
    {
      "name": "max",
      "type": "number",
      "required": false
    },
    {
      "name": "tone",
      "type": "Tone",
      "required": false,
      "values": [
        "primary",
        "danger",
        "success"
      ]
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "description": "Read out to a screen reader, e.g. \"3 unread\". Without it the marker is decorative (aria-hidden) — pass it whenever the count carries meaning."
    },
    {
      "name": "className",
      "type": "string",
      "required": false
    }
  ],
  "sourcePath": "src/components/CountBadge/CountBadge.tsx",
  "variants": {
    "tone": {
      "prop": "tone",
      "values": [
        "primary",
        "danger",
        "success"
      ]
    },
    "dot": {
      "prop": "dot",
      "values": [
        "true"
      ]
    }
  },
  "uses": [
    "IconButton",
    "Tooltip"
  ],
  "example": "/* A count pinned to a control. Pass `label` so the number is announced; use\n * `dot` for a plain unread marker. This is NOT <Badge> (a standalone pill). */\n\n<CountBadge count={5} label=\"5 notifications\">\n  <Tooltip content=\"Notifications\">\n    <IconButton icon=\"message\" aria-label=\"Notifications\" />\n  </Tooltip>\n</CountBadge>"
}
