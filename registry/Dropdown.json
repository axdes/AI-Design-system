{
  "ref": "Dropdown",
  "level": "molecule",
  "description": "Menu hung on a trigger you supply: keyboard navigation, portal and ARIA are built in, so spread `triggerProps` onto your own button. Items, sections and dividers are the parts.",
  "exports": [
    "Dropdown",
    "DropdownItem",
    "DropdownSection",
    "DropdownDivider"
  ],
  "main": "Dropdown",
  "from": "@/components/Dropdown",
  "slots": [
    {
      "name": "DropdownItem",
      "line": 260
    },
    {
      "name": "DropdownSection",
      "line": 295
    },
    {
      "name": "DropdownDivider",
      "line": 306
    }
  ],
  "props": [
    {
      "name": "trigger",
      "type": "(props: DropdownTriggerProps) => ReactNode",
      "required": true
    },
    {
      "name": "children",
      "type": "ReactNode",
      "required": true
    },
    {
      "name": "align",
      "type": "Align",
      "required": false,
      "values": [
        "start",
        "end"
      ]
    },
    {
      "name": "className",
      "type": "string",
      "required": false
    },
    {
      "name": "menuClassName",
      "type": "string",
      "required": false
    },
    {
      "name": "closeOnSelect",
      "type": "boolean",
      "required": false
    },
    {
      "name": "matchTriggerWidth",
      "type": "boolean | 'min'",
      "required": false
    }
  ],
  "sourcePath": "src/components/Dropdown/Dropdown.tsx",
  "context": "region",
  "uses": [
    "Button",
    "Icon",
    "Tooltip"
  ],
  "example": "<Dropdown\n  align=\"end\"\n  trigger={({ isOpen, ...triggerProps }) => (\n    <Button data-open={isOpen || undefined} {...triggerProps}>\n      Actions\n      <Icon name=\"arrow_drop_down\" />\n    </Button>\n  )}\n>\n  {/* Label a section once the menu holds more than a handful of actions. */}\n  <DropdownSection label=\"Edit\">\n    <DropdownItem icon=\"edit\" shortcut=\"⌘E\" onClick={() => undefined}>Rename</DropdownItem>\n    {/* Disabled items stay hoverable so the tooltip can say why. */}\n    <Tooltip content=\"Only the owner can move this\">\n      <DropdownItem icon=\"folder\" disabled>Move to folder</DropdownItem>\n    </Tooltip>\n  </DropdownSection>\n  {/* Destructive last, behind a divider: nothing to misclick on the way. */}\n  <DropdownDivider />\n  <DropdownItem icon=\"delete\" tone=\"danger\" onClick={() => undefined}>Delete</DropdownItem>\n</Dropdown>"
}
