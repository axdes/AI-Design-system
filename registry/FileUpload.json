{
  "ref": "FileUpload",
  "level": "molecule",
  "description": "A drop area plus a native file picker, with a removable list of attached files. Wraps the raw <input type=\"file\"> (the one control with no DS equivalent) and adds drag-and-drop. The input stays the accessible control; the drop area is a labelled convenience on top. Copy: the hint states the limits BEFORE the choosing: which formats, what size, how many. A rule discovered by failing is a rule stated too late.",
  "exports": [
    "FileUpload"
  ],
  "main": "FileUpload",
  "from": "@/components/FileUpload",
  "slots": [],
  "props": [
    {
      "name": "onFiles",
      "type": "(files: File[]) => void",
      "required": true,
      "description": "Called with the picked/dropped files (appended, not replaced)."
    },
    {
      "name": "files",
      "type": "File[]",
      "required": false,
      "description": "Currently attached files, rendered as a removable list."
    },
    {
      "name": "onRemove",
      "type": "(index: number) => void",
      "required": false
    },
    {
      "name": "accept",
      "type": "string",
      "required": false
    },
    {
      "name": "multiple",
      "type": "boolean",
      "required": false,
      "description": "Changes what the control IS: one file that replaces itself, or a set the reader adds to and removes from. Picking it because they might want several gives everyone a list to manage for a question with one answer."
    },
    {
      "name": "disabled",
      "type": "boolean",
      "required": false,
      "description": "Dimmed and unpressable, but pointer events are KEPT so a Tooltip can say why."
    },
    {
      "name": "invalid",
      "type": "boolean",
      "required": false,
      "description": "The form rejected the attachment (nothing picked where a file was required, or the wrong type). Red dashed border and a red focus ring."
    },
    {
      "name": "label",
      "type": "string",
      "required": false,
      "description": "Prompt shown in the drop area."
    },
    {
      "name": "hint",
      "type": "ReactNode",
      "required": false
    },
    {
      "name": "className",
      "type": "string",
      "required": false
    }
  ],
  "sourcePath": "src/components/FileUpload/FileUpload.tsx",
  "variants": {
    "invalid": {
      "prop": "invalid",
      "values": [
        "true"
      ]
    },
    "disabled": {
      "prop": "disabled",
      "values": [
        "true"
      ]
    }
  },
  "uses": [
    "Icon",
    "IconButton",
    "Tooltip"
  ],
  "example": "const [files, setFiles] = useState<File[]>([])<FileUpload\n  multiple\n  hint=\"PDF or PNG, up to 10 MB\"\n  files={files}\n  onFiles={(picked) => setFiles((cur) => [...cur, ...picked])}\n  onRemove={(i) => setFiles((cur) => cur.filter((_, x) => x !== i))}\n/>"
}
