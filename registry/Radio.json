{
  "ref": "Radio",
  "level": "atom",
  "description": "Single custom radio, one option in a set where exactly one is chosen. Native <input type=\"radio\"> (so a shared `name` gives arrow-key navigation + form semantics for free), visually themed. Copy: options are parallel: same part of speech, same length, same grain. \"Weekly\" beside \"Every Monday at 9\" reads as two different questions.",
  "exports": [
    "Radio",
    "RadioGroup"
  ],
  "main": "Radio",
  "from": "@/components/Radio",
  "slots": [
    {
      "name": "RadioGroup"
    }
  ],
  "props": [
    {
      "name": "label",
      "type": "string",
      "required": false,
      "description": "Visible label beside the dot."
    },
    {
      "name": "size",
      "type": "'sm' | 'md' | 'lg'",
      "required": false,
      "description": "Dot size: sm / md (default) / lg.",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    {
      "name": "invalid",
      "type": "boolean",
      "required": false,
      "description": "The form rejected this set. Draws the red circle and the red focus ring — the VISUAL half only. The announcement belongs to `<RadioGroup invalid>`, because a single radio cannot carry `aria-invalid`: ARIA does not support it on `role=\"radio\"`, and \"nothing is chosen\" is a fact about the set, not about any one option in it."
    }
  ],
  "sourcePath": "src/components/Radio/Radio.tsx",
  "inherits": "Omit<InputHTMLAttributes<HTMLInputElement>, 'type' | 'size'>",
  "variants": {
    "size": {
      "prop": "size",
      "values": [
        "sm",
        "md",
        "lg"
      ]
    },
    "invalid": {
      "prop": "invalid",
      "values": [
        "true"
      ]
    }
  },
  "uses": [
    "Layout",
    "SectionLabel"
  ],
  "example": "const [plan, setPlan] = useState<Plan>('monthly')\nconst [primary, setPrimary] = useState('ada')\n\n<Stack gap={6}>\n  {/* `label` is the group's ACCESSIBLE name, not a visible one: it becomes\n      aria-label on the radiogroup. A group a reader can see needs the text\n      on the screen as well, which is what SectionLabel is for. */}\n  <Stack gap={2}>\n    <SectionLabel>Billing period</SectionLabel>\n    <RadioGroup<Plan>\n      name=\"plan\"\n      label=\"Billing period\"\n      value={plan}\n      onChange={setPlan}\n      options={[\n        { value: 'monthly', label: 'Monthly' },\n        { value: 'annual', label: 'Annual' },\n      ]}\n    />\n  </Stack>\n  {/* One radio per row: the rows are the list, and `name` is all that makes\n      them exclusive. A RadioGroup would have to own the rows to do this. */}\n  <Stack gap={2}>\n    <SectionLabel>Primary contact</SectionLabel>\n    {[\n      { id: 'ada', name: 'Ada Meridian' },\n      { id: 'ben', name: 'Ben Torres' },\n    ].map((person) => (\n      <Row key={person.id} gap={3} align=\"center\">\n        <Radio\n          name=\"primary-contact\"\n          size=\"sm\"\n          value={person.id}\n          checked={primary === person.id}\n          onChange={() ="
}
