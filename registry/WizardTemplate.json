{
  "ref": "WizardTemplate",
  "level": "block",
  "description": "The WIZARD page skeleton: a Stepper over one step at a time, Back/Next below, the last step submitting under the real event's name. Steps are freely navigable, and the final step is a `<WizardReview>` whose Change links jump straight back. Copy: the page title names the whole job, the step names are its parts — nouns, parallel, and the same word in the stepper as in the step's own heading.",
  "exports": [
    "WizardTemplate",
    "WizardReview",
    "WizardReviewRow"
  ],
  "main": "WizardTemplate",
  "from": "@/blocks/WizardTemplate",
  "slots": [
    {
      "name": "WizardReview",
      "props": [
        {
          "name": "children",
          "type": "ReactNode",
          "required": false
        },
        {
          "name": "className",
          "type": "string",
          "required": false
        }
      ]
    },
    {
      "name": "WizardReviewRow"
    }
  ],
  "props": [
    {
      "name": "title",
      "type": "ReactNode",
      "required": false,
      "description": "Page title in the header."
    },
    {
      "name": "steps",
      "type": "WizardStep[]",
      "required": true,
      "description": "The ordered steps: 3-5. A 2-step wizard is a form with extra clicks."
    },
    {
      "name": "currentId",
      "type": "string",
      "required": true,
      "description": "Current step id — the parent owns it, usually mirrored into the URL."
    },
    {
      "name": "onSelect",
      "type": "(id: string) => void",
      "required": true,
      "description": "Back, Next and clicks on visited steps all arrive here."
    },
    {
      "name": "children",
      "type": "ReactNode",
      "required": false,
      "description": "The current step's content."
    },
    {
      "name": "backLabel",
      "type": "string",
      "required": false
    },
    {
      "name": "nextLabel",
      "type": "string",
      "required": false
    },
    {
      "name": "submitLabel",
      "type": "ReactNode",
      "required": true,
      "description": "Names the real event (\"Create schedule\"), never \"Submit\"."
    },
    {
      "name": "onSubmit",
      "type": "() => void",
      "required": true
    },
    {
      "name": "busy",
      "type": "boolean",
      "required": false,
      "description": "Submitting: the submit carries the spinner, the rest disables."
    },
    {
      "name": "className",
      "type": "string",
      "required": false
    }
  ],
  "sourcePath": "src/blocks/WizardTemplate/WizardTemplate.tsx",
  "context": "page",
  "uses": [
    "Button",
    "Field",
    "Input",
    "Select",
    "Stepper"
  ],
  "example": "const [step, setStep] = useState('details')\nconst [name, setName] = useState('Weekly export')\nconst [scope, setScope] = useState('all')\n\n<WizardTemplate\n  title=\"New export schedule\"\n  steps={STEPS}\n  currentId={step}\n  onSelect={setStep}\n  submitLabel=\"Create schedule\"\n  onSubmit={() => setStep('details')}\n>\n  {step === 'details' && (\n    <Field label=\"Schedule name\" htmlFor=\"wiz-name\">\n      <Input id=\"wiz-name\" value={name} onChange={(e) => setName(e.target.value)} />\n    </Field>\n  )}\n  {step === 'scope' && (\n    <Select\n      label=\"Scope\"\n      value={scope}\n      onChange={setScope}\n      options={[\n        { value: 'all', label: 'Whole workspace' },\n        { value: 'mine', label: 'My documents' },\n      ]}\n    />\n  )}\n  {step === 'review' && (\n    <WizardReview>\n      <WizardReviewRow label=\"Schedule name\" onEdit={() => setStep('details')}>\n        {name}\n      </WizardReviewRow>\n      <WizardReviewRow label=\"Scope\" onEdit={() => setStep('scope')}>\n        {scope === 'all' ? 'Whole workspace' : 'My documents'}\n      </WizardReviewRow>\n    </WizardReview>\n  )}\n</WizardTemplate>"
}
