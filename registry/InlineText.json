{
  "ref": "InlineText",
  "level": "molecule",
  "description": "True inline editing (the vanilla `.editable` contract): the SAME element becomes editable on click — same font, same layout, no control chrome. Enter commits, Escape reverts, blur saves. Key it by `value` upstream so a server-confirmed update re-renders the committed text. Copy: the accessible label names the value being edited, not the act — \"Recording title\", not \"Edit title\".",
  "exports": [
    "InlineText"
  ],
  "main": "InlineText",
  "from": "@/components/InlineText",
  "slots": [],
  "props": [
    {
      "name": "as",
      "type": "'h2' | 'p' | 'span'",
      "required": false,
      "description": "The element the value ALREADY is, not a size. A document title is the page's h2 whether or not it can be edited, so editing it must not take it out of the heading outline.",
      "values": [
        "h2",
        "p",
        "span"
      ]
    },
    {
      "name": "className",
      "type": "string",
      "required": false
    },
    {
      "name": "value",
      "type": "string",
      "required": true
    },
    {
      "name": "label",
      "type": "string",
      "required": true
    },
    {
      "name": "onSave",
      "type": "(next: string) => void",
      "required": true
    },
    {
      "name": "onAbort",
      "type": "() => void",
      "required": false,
      "description": "Called when editing ends without a change (composer rows close on this)."
    },
    {
      "name": "autoFocus",
      "type": "boolean",
      "required": false,
      "description": "Opens straight into the input. For the value the reader just created and is expected to name, not for a field they merely walked past."
    }
  ],
  "sourcePath": "src/components/InlineText/InlineText.tsx",
  "uses": [
    "Layout"
  ],
  "example": "const [title, setTitle] = useState('Untitled document')\nconst [note, setNote] = useState('Ships in 2 to 4 business days.')\n\n<Stack gap={6}>\n  \n  <InlineText as=\"h2\" value={title} label=\"Document title\" onSave={setTitle} />\n  \n  <InlineText as=\"p\" value={note} label=\"Delivery note\" onSave={setNote} />\n</Stack>"
}
