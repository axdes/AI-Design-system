{
  "ref": "PageHeader",
  "level": "organism",
  "description": "The top of a page: title, breadcrumb and the page-level actions. Page chrome belongs here, not in a screen's own CSS. The title stands alone by RULE: no text renders under it (a subtitle prop existed and was removed 2026-08-20 — explanatory lines under titles are banned from this system; what used to live there is content and belongs in the page body). Copy: the title is the record's own name, not its type — \"Northwind Paper\", not \"Supplier detail\". Nothing goes under it: an explanatory line there is content and belongs in the body.",
  "exports": [
    "PageHeader"
  ],
  "main": "PageHeader",
  "from": "@/components/PageHeader",
  "slots": [],
  "props": [
    {
      "name": "title",
      "type": "ReactNode",
      "required": false,
      "description": "Optional — omit for pages that center their own hero title instead."
    },
    {
      "name": "onBack",
      "type": "() => void",
      "required": false,
      "description": "Renders a leading circular back button before the title."
    },
    {
      "name": "backLabel",
      "type": "string",
      "required": false,
      "description": "Accessible label for the back button."
    },
    {
      "name": "breadcrumb",
      "type": "ReactNode",
      "required": false,
      "description": "Inline tools next to the title (search, filters)."
    },
    {
      "name": "inline",
      "type": "ReactNode",
      "required": false
    },
    {
      "name": "actions",
      "type": "ReactNode",
      "required": false,
      "description": "Trailing actions pinned to the far end (create button, user menu)."
    }
  ],
  "sourcePath": "src/components/PageHeader/PageHeader.tsx",
  "context": "region",
  "uses": [
    "Button",
    "Icon",
    "IconButton",
    "NavDrawerButton",
    "Tooltip"
  ],
  "example": "<PageHeader\n  title=\"Content library\"\n  actions={<Button variant=\"primary\">New document<Icon name=\"add\" /></Button>}\n/>"
}
