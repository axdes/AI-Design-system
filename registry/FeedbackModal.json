{
  "ref": "FeedbackModal",
  "level": "organism",
  "description": "\"What went wrong?\" — negative-feedback dialog (matches the original).",
  "exports": [
    "FeedbackModal"
  ],
  "main": "FeedbackModal",
  "from": "@/components/FeedbackModal",
  "slots": [],
  "props": [
    {
      "name": "open",
      "type": "boolean",
      "required": true
    },
    {
      "name": "onClose",
      "type": "() => void",
      "required": true
    },
    {
      "name": "onSubmit",
      "type": "(reason: Reason | null, details: string) => void",
      "required": true,
      "description": "Called with the picked reason + free-text details on send."
    },
    {
      "name": "labels",
      "type": "FeedbackLabels",
      "required": true,
      "fields": [
        {
          "name": "title",
          "type": "string",
          "required": true
        },
        {
          "name": "close",
          "type": "string",
          "required": true
        },
        {
          "name": "send",
          "type": "string",
          "required": true
        },
        {
          "name": "desc",
          "type": "string",
          "required": true
        },
        {
          "name": "detailsLabel",
          "type": "string",
          "required": true
        },
        {
          "name": "detailsPlaceholder",
          "type": "string",
          "required": true
        },
        {
          "name": "reasons",
          "type": "Record<Reason, string>",
          "required": true
        }
      ]
    }
  ],
  "sourcePath": "src/components/FeedbackModal/FeedbackModal.tsx",
  "context": "region",
  "uses": [
    "Button",
    "Chip",
    "Field",
    "Modal",
    "Textarea"
  ],
  "example": "const [open, setOpen] = useState(true)\n/* Every string comes from the caller: the component stays i18n-agnostic, so\n * the app passes already-translated copy (t('...')) into `labels`. */\n\n<FeedbackModal\n  open={open}\n  onClose={() => setOpen(false)}\n  onSubmit={() => setOpen(false)}\n  labels={{\n    title: 'What went wrong?',\n    close: 'Close',\n    send: 'Send feedback',\n    desc: 'Tell us what the answer got wrong so we can fix it.',\n    detailsLabel: 'Details',\n    detailsPlaceholder: 'What did you expect to see?',\n    reasons: {\n      wrong: 'Wrong answer',\n      incomplete: 'Incomplete',\n      unclear: 'Unclear',\n      other: 'Something else',\n    },\n  }}\n/>"
}
