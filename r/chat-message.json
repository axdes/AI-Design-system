{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "chat-message",
  "type": "registry:ui",
  "title": "ChatMessage",
  "description": "One chat message: a right-aligned bubble for the user, a left block for the assistant with a working toolbar (read aloud, copy, thumbs up/down) + optional quick-reply chips. Shared by every chat surface.",
  "dependencies": [
    "react-i18next"
  ],
  "registryDependencies": [
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/chip.json",
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/design-tokens.json",
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/icon-button.json",
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/tooltip.json"
  ],
  "files": [
    {
      "path": "src/components/ChatMessage/ChatMessage.css",
      "content": "/* Chat message — user bubble (right) / assistant block (left) + actions.\n * Fills the thread's centred column (the column is capped by the messages\n * container, like the original). */\n.chat-msg {\n  display: flex;\n  flex-direction: column;\n  width: 100%;\n}\n.chat-msg-user { align-items: flex-end; }\n\n.chat-bubble {\n  padding: var(--space-2) var(--space-4);\n  border-radius: var(--radius-lg);\n  background: var(--muted);\n  color: var(--foreground);\n  line-height: var(--leading-normal);\n}\n\n.chat-msg-body { display: flex; flex-direction: column; gap: var(--space-3); }\n.chat-msg-body p { margin: 0; line-height: var(--leading-normal); }\n\n/* Action toolbar — comfortable touch targets (md box) with small 16px glyphs,\n * like the original feedback row. Active (playing / copied / chosen) = primary. */\n.chat-msg-actions { display: flex; gap: var(--space-2xs); margin-block-start: var(--space-2); margin-inline-start: calc(-1 * var(--space-2)); }\n.chat-msg-actions .icon-button .icon { width: var(--icon-sm); height: var(--icon-sm); }\n.chat-msg-actions .icon-button[data-active],\n.chat-msg-actions .icon-button[data-active]:hover { color: var(--primary-accent); }\n\n/* Quick-reply chips (offered under an assistant message) — the default <Chip>. */\n.chat-choices { display: flex; flex-wrap: wrap; gap: var(--space-2); margin-block-start: var(--space-3); }\n",
      "type": "registry:ui",
      "target": "@ui/ChatMessage/ChatMessage.css"
    },
    {
      "path": "src/components/ChatMessage/ChatMessage.tsx",
      "content": "import './ChatMessage.css'\nimport { useEffect, useRef, useState, type ReactNode } from 'react'\nimport { useTranslation } from 'react-i18next'\nimport { Chip } from '../Chip'\nimport { IconButton } from '../IconButton'\nimport { Tooltip } from '../Tooltip'\n\n/** Quick-reply chip offered under an assistant message. */\nexport type ChatChoice = { label: string; onClick?: () => void }\n\ntype Props = {\n  role: 'user' | 'assistant'\n  /** Plain text of the message — used by copy + read-aloud on assistant rows. */\n  text?: string\n  /** Quick-reply chips under an assistant message. */\n  choices?: readonly ChatChoice[]\n  /** Hide the built-in action toolbar (copy / read / feedback). */\n  hideActions?: boolean\n  /** Fired when the user picks thumbs-down (e.g. to open a feedback dialog). */\n  onDislike?: () => void\n  children: ReactNode\n}\n\n/* One chat message: a right-aligned bubble for the user, a left block for the\n * assistant with a working toolbar (read aloud, copy, thumbs up/down) +\n * optional quick-reply chips. Shared by every chat surface. */\nexport function ChatMessage({ role, text, choices, hideActions, onDislike, children }: Props) {\n  const { t } = useTranslation()\n  const [copied, setCopied] = useState(false)\n  const [playing, setPlaying] = useState(false)\n  const [feedback, setFeedback] = useState<'up' | 'down' | null>(null)\n  const copyResetRef = useRef<number | null>(null)\n\n  /* Clear the pending copy-reset timer on unmount (no setState after teardown). */\n  useEffect(() => () => {\n    if (copyResetRef.current !== null) window.clearTimeout(copyResetRef.current)\n  }, [])\n\n  if (role === 'user') {\n    return (\n      <div className=\"chat-msg chat-msg-user\">\n        <div className=\"chat-bubble\">{children}</div>\n      </div>\n    )\n  }\n\n  const copy = () => {\n    if (text) navigator.clipboard?.writeText(text).catch(() => {})\n    setCopied(true)\n    if (copyResetRef.current !== null) window.clearTimeout(copyResetRef.current)\n    copyResetRef.current = window.setTimeout(() => setCopied(false), 1500)\n  }\n\n  const togglePlay = () => {\n    const synth = typeof window !== 'undefined' ? window.speechSynthesis : undefined\n    if (!synth || !text) { setPlaying((p) => !p); return }\n    if (playing) { synth.cancel(); setPlaying(false); return }\n    synth.cancel()\n    const u = new SpeechSynthesisUtterance(text)\n    u.onend = () => setPlaying(false)\n    synth.speak(u)\n    setPlaying(true)\n  }\n\n  const vote = (r: 'up' | 'down') => {\n    setFeedback((cur) => (cur === r ? null : r))\n    if (r === 'down' && feedback !== 'down') onDislike?.()\n  }\n\n  const listenLabel = playing ? t('agent.action.stop', { defaultValue: 'Stop' }) : t('agent.action.listen')\n  const copyLabel = copied ? t('agent.action.copied', { defaultValue: 'Copied' }) : t('agent.action.copy')\n\n  return (\n    <div className=\"chat-msg chat-msg-assistant\">\n      <div className=\"chat-msg-body\">{children}</div>\n      {choices && choices.length > 0 && (\n        <div className=\"chat-choices\">\n          {choices.map((c) => (\n            <Chip key={c.label} onClick={c.onClick}>{c.label}</Chip>\n          ))}\n        </div>\n      )}\n      {!hideActions && (\n        <div className=\"chat-msg-actions\">\n          <Tooltip content={listenLabel}>\n            <IconButton\n              icon={playing ? 'stop' : 'volume'}\n              size=\"md\"\n              aria-label={listenLabel}\n              aria-pressed={playing}\n              data-active={playing || undefined}\n              onClick={togglePlay}\n            />\n          </Tooltip>\n          <Tooltip content={copyLabel}>\n            <IconButton\n              icon={copied ? 'check' : 'content_copy'}\n              size=\"md\"\n              aria-label={copyLabel}\n              data-active={copied || undefined}\n              onClick={copy}\n            />\n          </Tooltip>\n          <Tooltip content={t('agent.action.like')}>\n            <IconButton\n              icon=\"thumb_up\"\n              size=\"md\"\n              aria-label={t('agent.action.like')}\n              aria-pressed={feedback === 'up'}\n              data-active={feedback === 'up' || undefined}\n              onClick={() => vote('up')}\n            />\n          </Tooltip>\n          <Tooltip content={t('agent.action.dislike')}>\n            <IconButton\n              icon=\"thumb_down\"\n              size=\"md\"\n              aria-label={t('agent.action.dislike')}\n              aria-pressed={feedback === 'down'}\n              data-active={feedback === 'down' || undefined}\n              onClick={() => vote('down')}\n            />\n          </Tooltip>\n        </div>\n      )}\n    </div>\n  )\n}\n",
      "type": "registry:ui",
      "target": "@ui/ChatMessage/ChatMessage.tsx"
    },
    {
      "path": "src/components/ChatMessage/index.ts",
      "content": "export { ChatMessage, type ChatChoice } from './ChatMessage'\n",
      "type": "registry:ui",
      "target": "@ui/ChatMessage/index.ts"
    }
  ],
  "docs": "Part of the AI Design System. Its full contract — every prop, its allowed values and what each one means — is at https://github.com/axdes/AI-Design-system/blob/main/registry/ChatMessage.json"
}
