{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "error-summary",
  "type": "registry:ui",
  "title": "ErrorSummary",
  "description": "The on-submit list of what the form rejected: focusable, counted, and each row links to its own field. Render it above the first field. Copy: the title counts the problems and nothing else — \"2 things need fixing\". Each row repeats the field's own error word for word, because a summary that paraphrases sends the reader hunting for a message that does not exist.",
  "dependencies": [
    "react-i18next"
  ],
  "registryDependencies": [
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/design-tokens.json"
  ],
  "files": [
    {
      "path": "src/components/ErrorSummary/ErrorSummary.css",
      "content": ".error-summary {\n  display: flex;\n  flex-direction: column;\n  gap: var(--space-2);\n  padding: var(--space-4);\n  border: 1px solid var(--destructive);\n  border-radius: var(--radius-md);\n  background: var(--background);\n}\n.error-summary:focus-visible {\n  outline: var(--ring-width) solid var(--ring);\n  outline-offset: var(--ring-offset);\n}\n\n.error-summary-title {\n  margin: 0;\n  font-size: var(--font-md);\n  font-weight: var(--font-weight-semibold);\n  line-height: var(--leading-snug);\n  color: var(--destructive-emphasis);\n}\n\n/* The ink is on the LIST, not only on the links inside it: a `::marker` takes\n * `color` from its own list item, so ink set on the anchor alone left every\n * bullet in the default foreground — a black dot beside a red line (owner,\n * 2026-08-24). */\n.error-summary-list {\n  margin: 0;\n  color: var(--destructive-emphasis);\n  padding-inline-start: var(--space-6);\n  display: flex;\n  flex-direction: column;\n  gap: var(--space-1);\n  font-size: var(--font-sm);\n  line-height: var(--leading-normal);\n}\n/* Rule 1 of the link system (Link.css): these sit in reading text and are the\n * only way to reach the field that failed, so they are underlined at rest and\n * they answer the pointer and the press like every other control. Added when\n * `npm run states` reported this as the one control in the system that did\n * neither; the component itself is somebody else's work in progress and is\n * otherwise untouched. */\n.error-summary-list a {\n  color: var(--destructive-emphasis);\n  text-decoration: underline;\n  text-underline-offset: 0.2em;\n  text-decoration-thickness: 1px;\n  border-radius: var(--radius-sm);\n  transition: text-decoration-color var(--duration-fast);\n}\n.error-summary-list a:hover { text-decoration-thickness: 2px; }\n.error-summary-list a:active { color: var(--destructive-emphasis); }\n.error-summary-list a:focus-visible {\n  outline: var(--ring-width) solid var(--ring);\n  outline-offset: var(--ring-offset);\n}\n",
      "type": "registry:ui",
      "target": "@ui/ErrorSummary/ErrorSummary.css"
    },
    {
      "path": "src/components/ErrorSummary/ErrorSummary.tsx",
      "content": "import './ErrorSummary.css'\nimport { useEffect, useId, useRef, type ReactNode } from 'react'\nimport { useTranslation } from 'react-i18next'\nimport { cn } from '../lib/cn'\n\nexport type FormError = {\n  /** The id of the control this failure belongs to. The row links to it. */\n  id: string\n  message: ReactNode\n}\n\ntype Props = {\n  /** The failures of the last submit, in the order the fields appear. */\n  errors: FormError[]\n  /** Heading above the list. Defaults to the count in the current language. */\n  title?: ReactNode\n  className?: string\n}\n\n/* What a failed submit owes a user who is not looking at the whole form: a\n * count, and a route. Marking each field in place is enough for someone who can\n * see the page at once, and useless for a keyboard user in a scrolled form and\n * for a screen reader that has already moved past the fields. So the summary\n * takes focus on submit, states how many failures there are, and every row is a\n * link that puts the caret in the field it names (GOV.UK error summary).\n *\n * The focus move is keyed on the errors themselves, not on mount: a second\n * failed submit with a different set has to pull focus back, or the second\n * attempt is silent. */\n\n/** The on-submit list of what the form rejected: focusable, counted, and each\n *  row links to its own field. Render it above the first field. \n *\n * Copy: the title counts the problems and nothing else — \"2 things need fixing\".\n * Each row repeats the field's own error word for word, because a summary\n * that paraphrases sends the reader hunting for a message that does not\n * exist.\n */\nexport function ErrorSummary({ errors, title, className }: Props) {\n  const { t } = useTranslation()\n  const ref = useRef<HTMLDivElement>(null)\n  /* Generated, not fixed: two forms on one screen (a page form and a panel)\n   * would otherwise share one id and the label would point at the wrong one. */\n  const titleId = useId()\n  const signature = errors.map((e) => e.id).join('|')\n\n  useEffect(() => {\n    if (!errors.length) return\n    ref.current?.focus()\n    /* Focus alone scrolls the nearest scroller, and inside a form card that is\n     * the card — but only if the browser agrees the element is out of view.\n     * Asking explicitly is what makes the summary visible when the submit was\n     * pressed from the bottom of a long form. */\n    ref.current?.scrollIntoView({ block: 'nearest' })\n    /* The ids are the identity of this failure set: re-focusing on every render\n     * would steal the caret while the user is fixing the first field. */\n    // eslint-disable-next-line react-hooks/exhaustive-deps\n  }, [signature])\n\n  if (!errors.length) return null\n\n  const focusField = (id: string) => {\n    const el = document.getElementById(id)\n    if (!(el instanceof HTMLElement)) return\n    el.focus()\n    el.scrollIntoView({ block: 'center', behavior: 'smooth' })\n  }\n\n  return (\n    <div\n      ref={ref}\n      className={cn('error-summary', className)}\n      tabIndex={-1}\n      /* `alert` announces it the moment it appears, which is the moment the\n       * user pressed the button and is waiting to hear what happened. */\n      role=\"alert\"\n      aria-labelledby={titleId}\n    >\n      <h2 id={titleId} className=\"error-summary-title\">\n        {title ?? t('field.problemTitle', { count: errors.length })}\n      </h2>\n      <ul className=\"error-summary-list\">\n        {errors.map((e) => (\n          <li key={e.id}>\n            {/* A real anchor, so it is in the link list and the URL fragment\n              * survives a copied link; the handler adds the focus the fragment\n              * alone does not give a control that is not tabbable by id. */}\n            <a\n              href={`#${e.id}`}\n              onClick={(event) => {\n                event.preventDefault()\n                focusField(e.id)\n              }}\n            >\n              {e.message}\n            </a>\n          </li>\n        ))}\n      </ul>\n    </div>\n  )\n}\n",
      "type": "registry:ui",
      "target": "@ui/ErrorSummary/ErrorSummary.tsx"
    },
    {
      "path": "src/components/ErrorSummary/index.ts",
      "content": "export { ErrorSummary, type FormError } from './ErrorSummary'\n",
      "type": "registry:ui",
      "target": "@ui/ErrorSummary/index.ts"
    }
  ],
  "docs": "Part of the AI Design System. Its full contract — every prop, its allowed values and what each one means — is at https://github.com/axdes/AI-Design-system/blob/main/registry/ErrorSummary.json"
}
