{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "file-upload",
  "type": "registry:ui",
  "title": "FileUpload",
  "description": "A drop area plus a native file picker, with a removable list of attached files. Wraps the raw <input type=\"file\"> (the one control with no DS equivalent) and adds drag-and-drop. The input stays the accessible control; the drop area is a labelled convenience on top. Copy: the hint states the limits BEFORE the choosing: which formats, what size, how many. A rule discovered by failing is a rule state",
  "registryDependencies": [
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/design-tokens.json",
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/icon.json",
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/icon-button.json",
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/tooltip.json"
  ],
  "files": [
    {
      "path": "src/components/FileUpload/FileUpload.css",
      "content": ".file-upload { display: flex; flex-direction: column; gap: var(--space-3); }\n\n.file-upload-drop {\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  gap: var(--space-1);\n  padding: var(--space-6);\n  border: 1px dashed var(--input);\n  border-radius: var(--radius-lg);\n  background: var(--muted);\n  text-align: center;\n  cursor: pointer;\n  transition: border-color var(--duration-fast), background var(--duration-fast);\n}\n/* Hover and press, like every other control. The drop area answered a DRAG\n * (data-over) and nothing else, so a pointer that had not picked anything up\n * yet got no sign the area was operable at all. */\n.file-upload-drop:hover:not([data-disabled]) { border-color: var(--ring); background: var(--accent-soft); }\n.file-upload-drop:active:not([data-disabled]) { border-color: var(--primary-active); }\n.file-upload-drop[data-over] { border-color: var(--primary); background: var(--accent-soft); }\n.file-upload-drop[data-invalid] { border-color: var(--destructive); }\n.file-upload-drop[data-invalid]:focus-within { outline-color: var(--destructive); }\n.file-upload-drop[data-disabled] { opacity: var(--disabled-opacity); cursor: not-allowed; }\n.file-upload-drop:focus-within {\n  outline: var(--focus-ring);\n  outline-offset: var(--ring-offset);\n}\n.file-upload-icon { color: var(--muted-foreground); }\n.file-upload-label { font-size: var(--font-base); font-weight: var(--font-weight-medium); color: var(--foreground); }\n.file-upload-hint { font-size: var(--font-sm); color: var(--muted-foreground); }\n\n/* The real control is present but visually collapsed; the label drives it. */\n.file-upload-input {\n  position: absolute;\n  width: 1px; height: 1px; padding: 0; margin: -1px;\n  overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;\n}\n\n.file-upload-list { margin: 0; padding: 0; list-style: none; display: flex; flex-direction: column; gap: var(--space-1); }\n.file-upload-file {\n  display: flex;\n  align-items: center;\n  gap: var(--space-2);\n  padding: var(--space-2) var(--space-3);\n  border: var(--surface-edge);\n  border-radius: var(--radius-md);\n  background: var(--card);\n}\n.file-upload-file-icon { color: var(--muted-foreground); }\n.file-upload-file-name { flex: 1; min-inline-size: 0; font-size: var(--font-sm); color: var(--foreground); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }\n",
      "type": "registry:ui",
      "target": "@ui/FileUpload/FileUpload.css"
    },
    {
      "path": "src/components/FileUpload/FileUpload.tsx",
      "content": "import './FileUpload.css'\nimport { useId, useRef, useState, type DragEvent , type ReactNode } from 'react'\nimport { cn } from '../lib/cn'\nimport { Icon } from '../Icon'\nimport { IconButton } from '../IconButton'\nimport { Tooltip } from '../Tooltip'\n\n/* Monolithic because it wraps the one control this system does not own — the\n * raw <input type=\"file\"> — and everything else here is that input's\n * contract: what it accepts, how many, its state, and the list of what has\n * been chosen. */\ntype Props = {\n  /** Called with the picked/dropped files (appended, not replaced). */\n  onChange: (files: File[]) => void\n  /** Currently attached files, rendered as a removable list. */\n  files?: File[]\n  onRemove?: (index: number) => void\n  accept?: string\n  /** Changes what the control IS: one file that replaces itself, or a set the reader adds to and\n   *  removes from. Picking it because they might want several gives everyone a list to manage\n   *  for a question with one answer.\n   */\n  multiple?: boolean\n  /** Dimmed and unpressable, but pointer events are KEPT so a Tooltip can say why. */\n  disabled?: boolean\n  /** The form rejected the attachment (nothing picked where a file was\n   *  required, or the wrong type). Red dashed border and a red focus ring. */\n  invalid?: boolean\n  /** Prompt shown in the drop area. */\n  label?: string\n  /* ReactNode: the same word means the same shape everywhere in this system,\n   * and its neighbours already took one. Widened 2026-09-03; it is rendered\n   * as content here, never put in an attribute. */\n  hint?: ReactNode\n  className?: string\n}\n\n/* A drop area plus a native file picker, with a removable list of attached\n * files. Wraps the raw <input type=\"file\"> (the one control with no DS\n * equivalent) and adds drag-and-drop. The input stays the accessible control;\n * the drop area is a labelled convenience on top. \n   *\n   * Copy: the hint states the limits BEFORE the choosing: which formats, what\n   * size, how many. A rule discovered by failing is a rule stated too late.\n   */\nexport function FileUpload({\n  onChange, files = [], onRemove, accept, multiple, disabled, invalid,\n  label = 'Drag files here or browse', hint, className,\n}: Props) {\n  const inputRef = useRef<HTMLInputElement>(null)\n  const [over, setOver] = useState(false)\n  const id = useId()\n\n  const handle = (list: FileList | null) => {\n    if (!list || !list.length) return\n    onChange(Array.from(list))\n  }\n  const onDrop = (e: DragEvent) => {\n    e.preventDefault()\n    setOver(false)\n    if (!disabled) handle(e.dataTransfer.files)\n  }\n\n  return (\n    <div className={cn('file-upload', className)}>\n      <label\n        className=\"file-upload-drop\"\n        data-invalid={invalid || undefined}\n        htmlFor={id}\n        data-over={over || undefined}\n        data-disabled={disabled || undefined}\n        onDragOver={(e) => { e.preventDefault(); if (!disabled) setOver(true) }}\n        onDragLeave={() => setOver(false)}\n        onDrop={onDrop}\n      >\n        <Icon name=\"upload\" size=\"lg\" className=\"file-upload-icon\" />\n        <span className=\"file-upload-label\">{label}</span>\n        {hint && <span className=\"file-upload-hint\">{hint}</span>}\n        <input\n          ref={inputRef}\n          id={id}\n          type=\"file\"\n          className=\"file-upload-input\"\n          accept={accept}\n          multiple={multiple}\n          disabled={disabled}\n          onChange={(e) => { handle(e.target.files); e.target.value = '' }}\n        />\n      </label>\n\n      {files.length > 0 && (\n        <ul className=\"file-upload-list\">\n          {files.map((file, i) => (\n            // eslint-disable-next-line @eslint-react/no-array-index-key -- name alone is not unique (a user can attach two files with the same name from different folders), so the index is the tiebreaker\n          <li className=\"file-upload-file\" key={`${file.name}-${i}`}>\n              <Icon name=\"insert_drive_file\" size=\"sm\" className=\"file-upload-file-icon\" />\n              <span className=\"file-upload-file-name\">{file.name}</span>\n              {onRemove && (\n                <Tooltip content=\"Remove\">\n                  <IconButton icon=\"close\" size=\"sm\" variant=\"quiet\" aria-label={`Remove ${file.name}`} onClick={() => onRemove(i)} />\n                </Tooltip>\n              )}\n            </li>\n          ))}\n        </ul>\n      )}\n    </div>\n  )\n}\n",
      "type": "registry:ui",
      "target": "@ui/FileUpload/FileUpload.tsx"
    },
    {
      "path": "src/components/FileUpload/index.ts",
      "content": "export { FileUpload } from './FileUpload'\n",
      "type": "registry:ui",
      "target": "@ui/FileUpload/index.ts"
    }
  ],
  "docs": "Part of the AI Design System. Its full contract — every prop, its allowed values and what each one means — is at https://github.com/axdes/AI-Design-system/blob/main/registry/FileUpload.json"
}
