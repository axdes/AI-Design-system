{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "rich-message",
  "type": "registry:ui",
  "title": "RichMessage",
  "description": "A message or AI answer as it was written: headings, quotes, code, lists and tables from TYPED blocks — never from HTML, so there is nothing to sanitise and nothing to inject. A table scrolls inside its own container; a thread that scrolls sideways has stopped being readable.",
  "registryDependencies": [
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/design-tokens.json"
  ],
  "files": [
    {
      "path": "src/components/RichMessage/RichMessage.css",
      "content": "/* As it was WRITTEN: a message's own line breaks survive, and a long token\n * (a url, a path) wraps rather than widening the column. */\n.rich-message-text {\n  margin: 0;\n  white-space: pre-wrap;\n  overflow-wrap: anywhere;\n}\n\n.rich-message-text + .rich-message-text {\n  margin-block-start: var(--space-2);\n}\n\n.rich-message-heading {\n  display: block;\n  font-weight: var(--font-weight-semibold);\n  margin-block: var(--space-3) var(--space-1);\n}\n\n.rich-message-rule {\n  border: 0;\n  border-block-start: 1px solid var(--border);\n  margin-block: var(--space-3);\n}\n\n.rich-message-quote {\n  margin: var(--space-2) 0;\n  padding-inline-start: var(--space-3);\n  border-inline-start: 2px solid var(--border);\n  color: var(--muted-foreground);\n}\n\n/* Code WRAPS rather than scrolls: a command in a message is one line the\n * reader should see whole without dragging it sideways. Tables scroll because\n * their columns only make sense aligned; code has no columns to protect. */\n.rich-message-code {\n  margin: var(--space-2) 0;\n  padding: var(--space-3);\n  background: var(--muted);\n  border-radius: var(--radius-md);\n  white-space: pre-wrap;\n  overflow-wrap: anywhere;\n  font-size: var(--font-sm);\n}\n\n.rich-message-inline-code {\n  padding-inline: var(--space-1);\n  background: var(--muted);\n  border-radius: var(--radius-sm);\n  font-size: var(--font-sm);\n}\n\n.rich-message-list {\n  margin: var(--space-2) 0;\n  padding-inline-start: var(--space-6);\n}\n\n/* Its own scroller: a wide table must not widen the message column. */\n.rich-message-table-scroll {\n  overflow-x: auto;\n  margin-block: var(--space-2);\n}\n\n.rich-message-table {\n  border-collapse: collapse;\n  font-size: var(--font-sm);\n}\n\n.rich-message-table th,\n.rich-message-table td {\n  padding: var(--space-1) var(--space-3);\n  border: 1px solid var(--border);\n  text-align: start;\n  /* nowrap is what makes the scroll container useful: cells hold their line\n   * and the TABLE moves, instead of every column squeezing into hyphens. */\n  white-space: nowrap;\n}\n\n.rich-message-table th {\n  font-weight: var(--font-weight-semibold);\n  background: var(--muted);\n}\n",
      "type": "registry:ui",
      "target": "@ui/RichMessage/RichMessage.css"
    },
    {
      "path": "src/components/RichMessage/RichMessage.tsx",
      "content": "import './RichMessage.css'\nimport { type ReactNode } from 'react'\nimport { cn } from '../lib/cn'\n\n/* The shapes a producer sends INSTEAD of markup. Nothing here ever receives\n * HTML, so nothing here sanitises any — the contract its first consumer proved\n * against real Teams traffic. */\nexport type RichBlock =\n  | { t: 'p'; text: string }\n  | { t: 'h'; text: string }\n  | { t: 'hr' }\n  | { t: 'quote'; text: string }\n  | { t: 'code'; text: string }\n  | { t: 'li'; items: string[]; ordered?: boolean }\n  | { t: 'table'; rows: string[][]; head?: boolean }\n\n/* Inline marks: **bold**, `code`, *emphasis*. Marks, not tags, so a message\n * that literally contains asterisks still renders as written. */\nconst MARK = /(\\*\\*[^*\\n]+\\*\\*|`[^`\\n]+`|\\*[^*\\n]+\\*)/g\n\nfunction inline(text: string): ReactNode[] {\n  return String(text).split(MARK).filter(Boolean).map((part, i) => {\n    if (part.length > 4 && part.startsWith('**') && part.endsWith('**')) return <strong key={i}>{part.slice(2, -2)}</strong>\n    if (part.length > 2 && part.startsWith('`') && part.endsWith('`')) return <code key={i} className=\"rich-message-inline-code\">{part.slice(1, -1)}</code>\n    if (part.length > 2 && part.startsWith('*') && part.endsWith('*')) return <em key={i}>{part.slice(1, -1)}</em>\n    return part\n  })\n}\n\ntype Props = {\n  /** The message as typed blocks. Absent (an old cache, a plain producer) falls back to `text` as one paragraph. */\n  blocks?: RichBlock[]\n  /** The flat text, always present — the words survive even when the shapes did not. */\n  text: string\n  className?: string\n}\n\n/**\n * A message or AI answer as it was written: headings, quotes, code, lists and\n * tables from TYPED blocks — never from HTML, so there is nothing to sanitise\n * and nothing to inject. A table scrolls inside its own container; a thread\n * that scrolls sideways has stopped being readable.\n */\nexport function RichMessage({ blocks, text, className }: Props) {\n  if (!blocks?.length) return <p className={cn('rich-message-text', className)}>{text}</p>\n  return (\n    <div className={className}>\n      {blocks.map((b, i) => {\n        const key = `${b.t}-${i}`\n        if (b.t === 'h') return <b key={key} className=\"rich-message-heading\">{inline(b.text)}</b>\n        if (b.t === 'hr') return <hr key={key} className=\"rich-message-rule\" />\n        if (b.t === 'quote') return <blockquote key={key} className=\"rich-message-quote\">{inline(b.text)}</blockquote>\n        if (b.t === 'code') return <pre key={key} className=\"rich-message-code\"><code>{b.text}</code></pre>\n        if (b.t === 'li') {\n          const List = b.ordered ? 'ol' : 'ul'\n          return <List key={key} className=\"rich-message-list\">{b.items.map((item, n) => <li key={n}>{inline(item)}</li>)}</List>\n        }\n        if (b.t === 'table') {\n          const head = b.head ? b.rows[0] : null\n          const rest = b.head ? b.rows.slice(1) : b.rows\n          return (\n            <div key={key} className=\"rich-message-table-scroll\">\n              <table className=\"rich-message-table\">\n                {head && <thead><tr>{head.map((c, n) => <th key={n}>{inline(c)}</th>)}</tr></thead>}\n                <tbody>{rest.map((row, n) => <tr key={n}>{row.map((c, j) => <td key={j}>{inline(c)}</td>)}</tr>)}</tbody>\n              </table>\n            </div>\n          )\n        }\n        return <p key={key} className=\"rich-message-text\">{inline(b.text)}</p>\n      })}\n    </div>\n  )\n}\n",
      "type": "registry:ui",
      "target": "@ui/RichMessage/RichMessage.tsx"
    },
    {
      "path": "src/components/RichMessage/index.ts",
      "content": "export { RichMessage, type RichBlock } from './RichMessage'\n",
      "type": "registry:ui",
      "target": "@ui/RichMessage/index.ts"
    }
  ],
  "docs": "Part of the AI Design System. Its full contract — every prop, its allowed values and what each one means — is at https://github.com/axdes/AI-Design-system/blob/main/registry/RichMessage.json"
}
