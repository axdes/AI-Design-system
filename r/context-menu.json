{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "context-menu",
  "type": "registry:ui",
  "title": "ContextMenu",
  "description": "A right-click menu positioned at the pointer. Opens on contextmenu over the wrapped target, clamps to the viewport, closes on outside-click, Escape or selection. Arrow keys move between items. Distinct from <Dropdown> (a left-click trigger button).",
  "dependencies": [
    "react-dom"
  ],
  "registryDependencies": [
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/design-tokens.json",
    "https://raw.githubusercontent.com/axdes/AI-Design-system/main/r/icon.json"
  ],
  "files": [
    {
      "path": "src/components/ContextMenu/ContextMenu.css",
      "content": ".context-target { display: contents; }\n.context-menu {\n  position: fixed;\n  z-index: var(--z-dropdown);\n  min-inline-size: 12rem;\n  padding: var(--space-1);\n  background: var(--popover);\n\n  border: 1px solid var(--border);\n  border-radius: var(--radius-lg);\n  box-shadow: var(--shadow-lg);\n  outline: none;\n}\n.context-item {\n  display: flex;\n  align-items: center;\n  gap: var(--space-3);\n  inline-size: 100%;\n  min-block-size: var(--control-height);\n  padding-inline: var(--space-3);\n  border: none;\n  border-radius: var(--radius-md);\n  background: transparent;\n  color: var(--popover-foreground);\n  font-family: inherit;\n  font-size: var(--font-base);\n  text-align: start;\n  cursor: pointer;\n}\n/* Stated twice on purpose: `data-active` is the keyboard position and is also\n * set on mouseenter, but a stylesheet that only names data-active reads as a\n * menu with no hover at all. */\n.context-item:hover:not(:disabled) { background: var(--surface-hover); }\n.context-item[data-active] { background: var(--surface-hover); }\n.context-item:active:not(:disabled) { background: var(--accent-soft); }\n.context-item[data-tone='destructive'] { color: var(--destructive-emphasis); }\n.context-item[data-tone='destructive'][data-active] { background: var(--destructive-soft); }\n.context-item-icon { color: var(--muted-foreground); }\n.context-item[data-tone='destructive'] .context-item-icon { color: var(--destructive-emphasis); }\n.context-item:disabled { opacity: 0.45; cursor: not-allowed; }\n\n/* WHERE ENTER WILL LAND. The fill alone cannot say it: on this surface a tint\n * measures 1.15:1 in light and 1.33 in dark, and SC 2.4.11 asks a focus\n * indicator for 3:1. `data-active` is set by the arrow keys AND by the pointer,\n * and in both cases it names the row Enter fires — so the mark is the same for\n * both. A pseudo-element, not an inset box-shadow: a shadow offset is physical\n * and would sit on the wrong side in Arabic. */\n.context-item[data-active] { position: relative; }\n.context-item[data-active]::before {\n  content: \"\";\n  position: absolute;\n  inset-block: var(--space-1);\n  inset-inline-start: 0;\n  inline-size: var(--space-2xs);\n  border-radius: var(--radius-full);\n  background: var(--primary-accent);\n}\n",
      "type": "registry:ui",
      "target": "@ui/ContextMenu/ContextMenu.css"
    },
    {
      "path": "src/components/ContextMenu/ContextMenu.tsx",
      "content": "import './ContextMenu.css'\nimport { useEffect, useRef, useState, type KeyboardEvent, type MouseEvent, type ReactNode, useId } from 'react'\nimport { createPortal } from 'react-dom'\nimport { cn } from '../lib/cn'\nimport { useDismiss } from '../lib/useDismiss'\nimport { Icon, type IconName } from '../Icon'\n\nexport type ContextMenuItem = {\n  id: string\n  label: string\n  icon?: IconName\n  tone?: 'neutral' | 'destructive'\n  disabled?: boolean\n  onSelect: () => void\n}\n\ntype Props = {\n  /** The target that opens the menu on right-click. */\n  children: ReactNode\n  items: ContextMenuItem[]\n  className?: string\n}\n\nconst EDGE = 8\n\n/* A right-click menu positioned at the pointer. Opens on contextmenu over the\n * wrapped target, clamps to the viewport, closes on outside-click, Escape or\n * selection. Arrow keys move between items. Distinct from <Dropdown> (a\n * left-click trigger button). */\nexport function ContextMenu({ children, items, className }: Props) {\n  const [pos, setPos] = useState<{ x: number; y: number } | null>(null)\n  const menuRef = useRef<HTMLDivElement>(null)\n  const [active, setActive] = useState(0)\n  const menuId = useId()\n\n  const open = (e: MouseEvent) => {\n    e.preventDefault()\n    const mw = 220\n    const mh = items.length * 40 + 16\n    setPos({\n      x: Math.min(e.clientX, window.innerWidth - mw - EDGE),\n      y: Math.min(e.clientY, window.innerHeight - mh - EDGE),\n    })\n    setActive(0)\n  }\n  const close = () => setPos(null)\n\n  /* Outside press and Escape are `useDismiss`'s, shared with every other layer.\n     There is nothing to measure here — the position came from the pointer event\n     that opened the menu — which is why this uses the dismissal alone and not\n     the whole anchored-layer mechanism. */\n  useDismiss({ open: pos !== null, onClose: close, stays: [menuRef] })\n\n  /* Focus the menu so arrow keys work immediately. */\n  useEffect(() => {\n    if (pos) menuRef.current?.focus()\n  }, [pos])\n\n  const onKeyDown = (e: KeyboardEvent<HTMLDivElement>) => {\n    if (e.key === 'ArrowDown') { e.preventDefault(); setActive((i) => Math.min(i + 1, items.length - 1)) }\n    else if (e.key === 'ArrowUp') { e.preventDefault(); setActive((i) => Math.max(i - 1, 0)) }\n    else if (e.key === 'Enter' || e.key === ' ') {\n      e.preventDefault()\n      const item = items[active]\n      if (item && !item.disabled) { item.onSelect(); close() }\n    }\n  }\n\n  const select = (item: ContextMenuItem) => { if (!item.disabled) { item.onSelect(); close() } }\n\n  return (\n    <>\n      <div className={cn('context-target', className)} onContextMenu={open}>\n        {children}\n      </div>\n      {pos && createPortal(\n        <div\n          ref={menuRef}\n          className=\"context-menu\"\n          data-raised=\"popover\"\n          role=\"menu\"\n          aria-label=\"Context menu\"\n          tabIndex={-1}\n          /* Focus stays on the menu and the ARROW KEYS move `active`, so the row\n           * the eye follows has to be named here or a screen reader announces\n           * nothing as the selection moves. The mark on that row (see the\n           * ::before in ContextMenu.css) is its visible half. */\n          aria-activedescendant={items[active] ? `${menuId}-${items[active].id}` : undefined}\n          style={{ insetInlineStart: pos.x, insetBlockStart: pos.y }}\n          onKeyDown={onKeyDown}\n        >\n          {items.map((item, i) => (\n            <button\n              key={item.id}\n              id={`${menuId}-${item.id}`}\n              type=\"button\"\n              role=\"menuitem\"\n              className=\"context-item\"\n              data-tone={item.tone}\n              data-active={i === active || undefined}\n              disabled={item.disabled}\n              onMouseEnter={() => setActive(i)}\n              onClick={() => select(item)}\n            >\n              {item.icon && <Icon name={item.icon} size=\"sm\" className=\"context-item-icon\" />}\n              {item.label}\n            </button>\n          ))}\n        </div>,\n        document.body,\n      )}\n    </>\n  )\n}\n",
      "type": "registry:ui",
      "target": "@ui/ContextMenu/ContextMenu.tsx"
    },
    {
      "path": "src/components/ContextMenu/index.ts",
      "content": "export { ContextMenu, type ContextMenuItem } from './ContextMenu'\n",
      "type": "registry:ui",
      "target": "@ui/ContextMenu/index.ts"
    }
  ],
  "docs": "Part of the AI Design System. Its full contract — every prop, its allowed values and what each one means — is at https://github.com/axdes/AI-Design-system/blob/main/registry/ContextMenu.json"
}
