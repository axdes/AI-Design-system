/* Golden example. A person in a list or a card head: face beside name.
 * `vertical` exists for the one screen where the profile IS the hero. */
import { Identity } from './Identity'

export function Example() {
  return <Identity name="Sarah Al-Mansouri" secondary="Product designer" size="lg" />
}
